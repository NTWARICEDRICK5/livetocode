import type { CourseDetailMap } from "@/data/courseDetailTypes";

const batch1: CourseDetailMap = {
  java: {
    intro:
      "Java is a strongly-typed, object-oriented language that runs on the JVM and powers everything from Android apps to enterprise banking systems. This track takes you from raw syntax to building and testing real Java applications with Maven or Gradle.",
    lessonNotes: {
      "Setting up the JDK": "Install a JDK, set `JAVA_HOME`, and compile/run a `HelloWorld.java` file with `javac` and `java` from the command line.",
      "Variables and types": "Declare primitives (`int`, `double`, `boolean`, `char`) versus reference types, and understand Java's static typing and default values.",
      "Control flow": "Write `if`/`else`, `switch` expressions, `for`, `while` and `do-while` loops to branch and repeat logic.",
      "Methods": "Define methods with typed parameters and return values, and practice method overloading within a class.",
      "Arrays": "Create and iterate fixed-size arrays with `int[]`/`String[]` syntax, including multi-dimensional arrays.",
      "Classes and objects": "Define a class with fields and a constructor, then instantiate objects with `new` and call their methods.",
      "Inheritance": "Extend a base class with `extends`, override methods with `@Override`, and call `super()` from a subclass constructor.",
      "Interfaces": "Declare an `interface` with abstract methods and implement it in multiple unrelated classes.",
      "Abstract classes": "Use `abstract class` to share partial implementation while forcing subclasses to implement specific methods.",
      "Records": "Model immutable data with `record` types, letting Java auto-generate constructors, `equals`, `hashCode` and accessors.",
      "List, Set, Map": "Store and retrieve data using `ArrayList`, `HashSet` and `HashMap` from `java.util`.",
      "Iterators": "Traverse collections safely with `Iterator` and enhanced for-loops, and remove elements during iteration with `iterator.remove()`.",
      "Generics": "Write a generic class or method with `<T>` to work with any type while keeping compile-time type safety.",
      "Streams API": "Transform collections declaratively with `stream().filter().map().collect()`.",
      "Optional": "Wrap potentially-absent values in `Optional<T>` and use `isPresent()`/`orElse()` instead of null checks.",
      "Exceptions": "Throw and catch checked and unchecked exceptions with `try`/`catch`/`finally`, and create a custom exception class.",
      "File I/O": "Read and write text files using `Files.readAllLines` and `BufferedWriter` from `java.nio.file`.",
      "Concurrency basics": "Start a new thread with `Thread` or `Runnable`, and synchronize shared state with the `synchronized` keyword.",
      "Maven & Gradle": "Configure a `pom.xml` or `build.gradle` to declare dependencies and run a build lifecycle like `mvn package`.",
      "Unit testing with JUnit": "Write test methods annotated with `@Test` and assert results with `assertEquals` using JUnit 5.",
    },
    exercises: [
      { title: "FizzBuzz with switch", prompt: "Print numbers 1–100, replacing multiples of 3 with 'Fizz', 5 with 'Buzz', and both with 'FizzBuzz', using a `switch` expression.", hint: "Use `n % 3 == 0 && n % 5 == 0` as your first case check." },
      { title: "Immutable Point record", prompt: "Create a `Point` record with `x` and `y` fields, then write a method that returns the distance between two points.", hint: "`record Point(double x, double y) {}` auto-generates accessors like `point.x()`." },
      { title: "Word frequency counter", prompt: "Read a sentence and use a `HashMap<String, Integer>` to count occurrences of each word.", hint: "Use `map.merge(word, 1, Integer::sum)` to increment counts." },
      { title: "Custom exception", prompt: "Write an `InsufficientFundsException` and throw it from a `withdraw` method when the amount exceeds the balance.", hint: "Extend `RuntimeException` and call `super(message)` in your constructor." },
    ],
    projects: [
      { title: "Console Library Catalog", brief: "A command-line app that stores books in memory and supports adding, searching and listing them.", steps: ["Model a `Book` class with title, author and ISBN", "Store books in an `ArrayList<Book>`", "Implement add/search/list menu options with a loop", "Handle invalid input with try/catch", "Add unit tests for the search logic"], skills: ["OOP", "Collections", "Exceptions", "JUnit"] },
      { title: "Bank Account Simulator", brief: "Simulate multiple accounts with deposits, withdrawals and transaction history.", steps: ["Define an `Account` class with balance and a `List<Transaction>`", "Implement deposit/withdraw with validation exceptions", "Use streams to compute total balance across accounts", "Persist accounts to a text file with file I/O", "Write JUnit tests covering edge cases"], skills: ["Streams", "File I/O", "Exceptions", "Testing"] },
      { title: "Multi-threaded Task Queue", brief: "A small job runner that processes tasks concurrently and reports results safely.", steps: ["Define a `Task` interface with a `run()` method", "Build a queue with `ConcurrentLinkedQueue`", "Spawn worker threads that pull and execute tasks", "Synchronize a shared results list", "Package the project with Maven and add tests"], skills: ["Concurrency", "Interfaces", "Maven", "Testing"] },
    ],
    resources: [
      { label: "Oracle Java Tutorials", url: "https://docs.oracle.com/javase/tutorial/" },
      { label: "Java Language Specification", url: "https://docs.oracle.com/javase/specs/" },
      { label: "JUnit 5 User Guide", url: "https://junit.org/junit5/docs/current/user-guide/" },
    ],
  },
  csharp: {
    intro:
      "C# is Microsoft's statically-typed, object-oriented language for building web APIs, desktop apps and games with .NET. This track covers core syntax through modern async, LINQ and minimal API patterns.",
    lessonNotes: {
      "Hello .NET": "Scaffold a console app with `dotnet new console` and run it with `dotnet run`.",
      "Types and variables": "Use built-in types like `int`, `string` and `var` for type inference, and understand value vs reference types.",
      "Control flow": "Write `if`, `switch` expressions with pattern matching, and `for`/`foreach` loops.",
      "Methods": "Declare methods with optional and named parameters, and use expression-bodied members.",
      "Namespaces": "Organize code into `namespace` blocks and reference them with `using` directives.",
      "Classes and structs": "Compare reference-type `class` with value-type `struct`, and choose the right one for a data model.",
      "Properties": "Define auto-implemented properties with `{ get; set; }` and add custom logic in a property backing field.",
      "Interfaces": "Declare an `interface` and implement it across multiple classes to enable polymorphism.",
      "Inheritance": "Extend a base class with `:`, override virtual methods, and call `base()` constructors.",
      "Records": "Model immutable data with `record` types that get value-based equality for free.",
      "Collections": "Work with `List<T>`, `Dictionary<TKey,TValue>` and `HashSet<T>` from `System.Collections.Generic`.",
      "LINQ queries": "Filter and project sequences using LINQ methods like `Where`, `Select` and `OrderBy`.",
      "Nullable types": "Use nullable reference types (`string?`) and the null-conditional operator `?.` to avoid `NullReferenceException`.",
      "JSON serialization": "Serialize and deserialize objects with `System.Text.Json.JsonSerializer`.",
      "File I/O": "Read and write files with `File.ReadAllText` and `StreamWriter`.",
      "Async and tasks": "Write asynchronous methods with `async`/`await` returning `Task<T>`.",
      "Dependency injection": "Register services in the built-in DI container and inject them via constructor parameters.",
      "Minimal APIs": "Define HTTP endpoints with `app.MapGet`/`MapPost` in a minimal API `Program.cs`.",
      "Testing": "Write unit tests with xUnit using `[Fact]` and `Assert.Equal`.",
      "Publishing": "Publish a self-contained deployment with `dotnet publish -c Release`.",
    },
    exercises: [
      { title: "Pattern-matching calculator", prompt: "Write a method that takes an operator string and two numbers, using a `switch` expression to return the result.", hint: "Use `_` as the default arm to throw for unknown operators." },
      { title: "LINQ top scorer", prompt: "Given a list of `Player` records with names and scores, use LINQ to find the top 3 scorers sorted descending.", hint: "Chain `.OrderByDescending(p => p.Score).Take(3)`." },
      { title: "Async file downloader", prompt: "Write an `async Task<string>` method that reads a local file's contents without blocking the calling thread.", hint: "Use `await File.ReadAllTextAsync(path)`." },
      { title: "Minimal API greeting", prompt: "Create a minimal API with a `GET /hello/{name}` endpoint that returns a JSON greeting.", hint: "`app.MapGet(\"/hello/{name}\", (string name) => new { message = $\"Hi {name}\" });`" },
    ],
    projects: [
      { title: "Console Expense Tracker", brief: "A CLI tool to log and summarize personal expenses stored in memory.", steps: ["Model an `Expense` record with amount, category and date", "Store expenses in a `List<Expense>`", "Use LINQ to group and total by category", "Serialize expenses to JSON on exit", "Add xUnit tests for the totals logic"], skills: ["Records", "LINQ", "JSON", "Testing"] },
      { title: "To-Do REST API", brief: "A minimal API backed by an in-memory list exposing CRUD endpoints for to-do items.", steps: ["Scaffold a minimal API project", "Define a `TodoItem` record", "Implement GET/POST/PUT/DELETE endpoints", "Add input validation and proper status codes", "Write integration tests with `WebApplicationFactory`"], skills: ["Minimal APIs", "DI", "Async", "Testing"] },
      { title: "Weather Dashboard Service", brief: "An async service that fetches and caches weather data from a public API.", steps: ["Register an `HttpClient` via dependency injection", "Call an external weather API asynchronously", "Cache responses with `IMemoryCache`", "Expose the data through a minimal API endpoint", "Publish the app as a self-contained deployment"], skills: ["Async", "DI", "Minimal APIs", "Publishing"] },
    ],
    resources: [
      { label: "Microsoft C# Documentation", url: "https://learn.microsoft.com/dotnet/csharp/" },
      { label: ".NET Minimal APIs Guide", url: "https://learn.microsoft.com/aspnet/core/fundamentals/minimal-apis" },
      { label: "xUnit Getting Started", url: "https://xunit.net/docs/getting-started/netcore/cmdline" },
    ],
  },
  go: {
    intro:
      "Go is Google's compiled, statically-typed language designed for simplicity and concurrency, widely used for cloud services and CLI tools. This track builds from syntax basics to writing concurrent, tested HTTP services.",
    lessonNotes: {
      "Installing Go": "Install the Go toolchain and run your first program with `go run main.go`.",
      "Variables and types": "Declare variables with `var` and `:=`, and use Go's basic types like `int`, `string` and `bool`.",
      "Functions": "Define functions with multiple return values, including the idiomatic `(result, error)` pattern.",
      "Structs": "Define a `struct` to group related fields and create instances with struct literals.",
      "Slices and maps": "Use `[]T` slices with `append` and `map[K]V` for key-value storage.",
      "Methods and interfaces": "Attach methods to types with receiver syntax, and satisfy an `interface` implicitly.",
      "Error handling": "Return and check `error` values idiomatically with `if err != nil`, and wrap errors with `fmt.Errorf(\"%w\")`.",
      "Packages and modules": "Organize code into packages and manage dependencies with `go.mod` via `go mod init`.",
      "Pointers": "Use `*T` and `&x` to pass and mutate values by reference.",
      "Generics": "Write generic functions with type parameters like `func Max[T constraints.Ordered](a, b T) T`.",
      "Goroutines": "Launch concurrent work with the `go` keyword and understand the goroutine scheduler.",
      "Channels": "Communicate between goroutines using `chan T` to send and receive values safely.",
      "select": "Use `select` to wait on multiple channel operations and implement timeouts.",
      "sync package": "Coordinate goroutines with `sync.WaitGroup` and protect shared state with `sync.Mutex`.",
      "Context": "Propagate cancellation and deadlines through call chains using `context.Context`.",
      "net/http basics": "Handle HTTP requests with `http.HandleFunc` and start a server via `http.ListenAndServe`.",
      "JSON APIs": "Encode and decode JSON payloads with `encoding/json`'s `Marshal`/`Unmarshal`.",
      "Middleware": "Wrap `http.Handler` functions to add logging or auth as reusable middleware.",
      "Testing": "Write table-driven tests using the standard `testing` package and `go test`.",
      "Deploying a Go binary": "Cross-compile a statically-linked binary with `GOOS`/`GOARCH` and deploy it without a runtime.",
    },
    exercises: [
      { title: "Word counter", prompt: "Write a function that takes a string and returns a `map[string]int` of word counts.", hint: "Use `strings.Fields(s)` to split on whitespace." },
      { title: "Concurrent sum", prompt: "Split a slice of numbers into chunks and sum each chunk in a goroutine, combining results over a channel.", hint: "Use a `sync.WaitGroup` to wait for all goroutines before reading results." },
      { title: "Custom error wrapping", prompt: "Write a function that reads a file and wraps any error with additional context using `fmt.Errorf(\"reading config: %w\", err)`.", hint: "Use `errors.Unwrap` or `errors.Is` in a test to verify wrapping works." },
      { title: "JSON echo handler", prompt: "Write an `http.HandlerFunc` that decodes a JSON body and re-encodes it back with an added timestamp field.", hint: "Use `json.NewDecoder(r.Body).Decode(&v)` then `json.NewEncoder(w).Encode(v)`." },
    ],
    projects: [
      { title: "CLI Task Manager", brief: "A command-line to-do tool that persists tasks to a JSON file.", steps: ["Define a `Task` struct with ID, title and done status", "Implement add/list/complete subcommands with `os.Args`", "Persist tasks by marshaling to a JSON file", "Handle errors idiomatically at each step", "Add table-driven tests for the task logic"], skills: ["Structs", "JSON", "Error handling", "Testing"] },
      { title: "Concurrent URL Checker", brief: "A tool that checks the status of many URLs concurrently and reports results.", steps: ["Read a list of URLs from a file", "Launch a goroutine per URL with a worker pool", "Use channels to collect status codes", "Apply a timeout per request via `context.WithTimeout`", "Print a summary report sorted by response time"], skills: ["Goroutines", "Channels", "Context", "net/http"] },
      { title: "REST API for Notes", brief: "An HTTP API with CRUD endpoints for notes backed by an in-memory store.", steps: ["Set up routes with `net/http` and a router mux", "Implement CRUD handlers returning JSON", "Add logging middleware wrapping all handlers", "Write integration tests using `httptest`", "Build and deploy a static binary"], skills: ["net/http", "Middleware", "Testing", "Deployment"] },
    ],
    resources: [
      { label: "The Go Tour", url: "https://go.dev/tour/" },
      { label: "Effective Go", url: "https://go.dev/doc/effective_go" },
      { label: "Go Standard Library Docs", url: "https://pkg.go.dev/std" },
    ],
  },
  rust: {
    intro:
      "Rust is a systems programming language that guarantees memory safety without a garbage collector through its ownership model. This track builds from Cargo basics to ownership, traits and concurrent, tested crates.",
    lessonNotes: {
      "Cargo and rustc": "Create a new project with `cargo new`, and understand how `cargo build`/`cargo run` invoke `rustc`.",
      "Variables and mutability": "Declare immutable bindings with `let` and opt into mutation with `let mut`.",
      "Types": "Use Rust's scalar types (`i32`, `f64`, `bool`, `char`) and compound types like tuples and arrays.",
      "Functions": "Define functions with typed parameters and an implicit final-expression return value.",
      "Control flow": "Write `if`/`else` as expressions, and use `loop`, `while` and `for` with ranges.",
      "Ownership rules": "Understand that each value has a single owner, and that assignment moves rather than copies non-`Copy` types.",
      "Borrowing": "Pass references with `&`/`&mut` to use a value without taking ownership, respecting the borrow checker's rules.",
      "Slices": "Use `&str` and `&[T]` slices to reference a contiguous portion of a string or array.",
      "Lifetimes": "Annotate function signatures with lifetime parameters like `<'a>` when returning borrowed references.",
      "Smart pointers": "Use `Box<T>`, `Rc<T>` and `RefCell<T>` to manage heap allocation and shared/interior mutability.",
      "Structs": "Define a `struct` with named fields and implement behavior in an `impl` block.",
      "Enums and pattern matching": "Model variants with `enum` and destructure them exhaustively with `match`.",
      "Traits": "Define shared behavior with `trait` and implement it for multiple types, including default methods.",
      "Generics": "Write generic functions and structs constrained by trait bounds like `<T: Display>`.",
      "Error handling": "Return `Result<T, E>` from fallible functions and propagate errors with the `?` operator.",
      "Modules and crates": "Organize code with `mod` and expose items with `pub`, and add dependencies via `Cargo.toml`.",
      "Collections": "Use `Vec<T>`, `HashMap<K, V>` and `HashSet<T>` from the standard library.",
      "Testing": "Write `#[test]` functions and run them with `cargo test`.",
      "Concurrency with threads": "Spawn OS threads with `std::thread::spawn` and share state safely with `Arc<Mutex<T>>`.",
      "async/await": "Write asynchronous functions with `async fn` and run them on a runtime like `tokio`.",
    },
    exercises: [
      { title: "Ownership puzzle", prompt: "Write a function that takes ownership of a `String`, appends text, and returns it, then call it and print the result.", hint: "Passing by value moves the `String`; return it to reuse the caller's variable." },
      { title: "Enum-based shape area", prompt: "Define an enum `Shape` with `Circle(f64)` and `Rectangle(f64, f64)` variants, and write a `match`-based `area` function.", hint: "`match shape { Shape::Circle(r) => ..., Shape::Rectangle(w, h) => ... }`" },
      { title: "Result-based parser", prompt: "Write a function that parses a string into an `i32`, returning a custom `Result<i32, ParseError>`.", hint: "Use `.map_err()` to convert `std::num::ParseIntError` into your own error type." },
      { title: "Thread-safe counter", prompt: "Increment a shared counter from 10 spawned threads and print the final value.", hint: "Wrap the counter in `Arc<Mutex<i32>>` and clone the `Arc` for each thread." },
    ],
    projects: [
      { title: "CLI Word Frequency Tool", brief: "A command-line tool that reads a text file and prints word frequencies sorted descending.", steps: ["Parse a file path from CLI args", "Read the file into a `String`", "Count words using a `HashMap<String, u32>`", "Sort and print results by frequency", "Add unit tests for the counting logic"], skills: ["Ownership", "Collections", "Error handling", "Testing"] },
      { title: "Mini Key-Value Store", brief: "An in-memory key-value store exposed as a library crate with a trait-based storage interface.", steps: ["Define a `Store` trait with get/set/delete methods", "Implement it with a `HashMap`-backed struct", "Support optional persistence to disk", "Handle errors with a custom `Result` type", "Write integration tests using `cargo test`"], skills: ["Traits", "Generics", "Error handling", "Modules"] },
      { title: "Concurrent Job Runner", brief: "A multi-threaded task runner that processes jobs from a shared queue.", steps: ["Model jobs as a trait object `Box<dyn Fn()>`", "Build a thread pool using `Arc<Mutex<VecDeque<Job>>>`", "Spawn worker threads that pull and execute jobs", "Add graceful shutdown signaling", "Benchmark and test with `cargo test`"], skills: ["Concurrency", "Smart pointers", "Traits", "Testing"] },
    ],
    resources: [
      { label: "The Rust Book", url: "https://doc.rust-lang.org/book/" },
      { label: "Rust by Example", url: "https://doc.rust-lang.org/rust-by-example/" },
      { label: "Cargo Guide", url: "https://doc.rust-lang.org/cargo/guide/" },
    ],
  },
  php: {
    intro:
      "PHP is a server-side scripting language purpose-built for the web, powering everything from WordPress to Laravel applications. This track goes from basic syntax to database-backed, tested PHP applications.",
    lessonNotes: {
      "Syntax and echo": "Write PHP inside `<?php ?>` tags and output content with `echo`/`print`.",
      "Variables and types": "Declare `$variables` and understand PHP's loose typing with `int`, `string`, `array` and `bool`.",
      "Control flow": "Use `if`/`elseif`/`else`, `switch`, and `for`/`foreach`/`while` loops.",
      "Functions": "Define functions with default parameter values and typed return declarations.",
      "Arrays": "Work with indexed and associative arrays using `[]` literals and functions like `array_map`.",
      "Forms and $_POST": "Read submitted form data from the `$_POST` superglobal in a handling script.",
      "Validation": "Validate and sanitize user input with functions like `filter_var`.",
      "Sessions and cookies": "Start a session with `session_start()` and store/retrieve values in `$_SESSION`, and set cookies with `setcookie`.",
      "File uploads": "Handle uploaded files via `$_FILES` and move them with `move_uploaded_file`.",
      "Includes and templates": "Split pages into reusable pieces with `include`/`require` for simple templating.",
      "PDO and MySQL": "Connect to MySQL using `PDO` and run queries against a database.",
      "Prepared statements": "Prevent SQL injection using `PDO::prepare` with bound parameters.",
      "CRUD app": "Build create, read, update and delete flows against a database table using PDO.",
      "Errors and logging": "Configure error reporting and log errors with `error_log` instead of displaying them to users.",
      "JSON APIs": "Return JSON responses from a PHP script using `json_encode` and set the correct content-type header.",
      "Composer": "Manage dependencies with `composer.json` and autoload classes via `composer require`.",
      "Namespaces": "Organize classes with `namespace` declarations and `use` imports.",
      "OOP in PHP": "Define classes with properties, constructors and methods, and use interfaces for polymorphism.",
      "PSR standards": "Follow PSR-4 autoloading and PSR-12 coding style conventions for interoperable code.",
      "Testing with PHPUnit": "Write test classes extending `TestCase` and assert behavior with `assertEquals`.",
    },
    exercises: [
      { title: "Array stats", prompt: "Write a function that takes an array of numbers and returns an associative array with min, max and average.", hint: "Use `min()`, `max()` and `array_sum()/count()`." },
      { title: "Form validator", prompt: "Validate a submitted email and age from `$_POST`, returning error messages for invalid input.", hint: "Use `filter_var($email, FILTER_VALIDATE_EMAIL)`." },
      { title: "Prepared statement lookup", prompt: "Write a PDO query that fetches a user by ID using a prepared statement with a bound parameter.", hint: "`$stmt = $pdo->prepare('SELECT * FROM users WHERE id = ?'); $stmt->execute([$id]);`" },
      { title: "PHPUnit class test", prompt: "Write a `Cart` class with an `addItem` method and a PHPUnit test asserting the total price is correct.", hint: "Extend `PHPUnit\\Framework\\TestCase` and use `assertEquals` on the computed total." },
    ],
    projects: [
      { title: "Contact Form Backend", brief: "A PHP script that validates and stores contact form submissions to a file.", steps: ["Build an HTML form posting to a PHP handler", "Validate inputs with `filter_var`", "Store valid submissions to a JSON or CSV file", "Show success/error messages back to the user", "Add basic protection against duplicate submissions via sessions"], skills: ["Forms", "Validation", "Sessions", "File I/O"] },
      { title: "Todo CRUD App", brief: "A database-backed to-do list with add, edit, complete and delete operations.", steps: ["Design a `todos` MySQL table", "Connect with PDO and prepared statements", "Implement create/read/update/delete pages", "Add basic error logging for failed queries", "Write PHPUnit tests for the data access layer"], skills: ["PDO", "Prepared statements", "CRUD", "Testing"] },
      { title: "JSON API with Composer", brief: "A small REST-style API for managing products, structured with namespaces and Composer autoloading.", steps: ["Set up a `composer.json` with PSR-4 autoloading", "Define namespaced model and repository classes", "Implement endpoints that return `json_encode`d responses", "Follow PSR-12 style conventions throughout", "Cover the repository logic with PHPUnit tests"], skills: ["Composer", "OOP", "JSON APIs", "PSR standards"] },
    ],
    resources: [
      { label: "PHP Manual", url: "https://www.php.net/manual/en/" },
      { label: "PHP The Right Way", url: "https://phptherightway.com/" },
      { label: "PHPUnit Documentation", url: "https://docs.phpunit.de/en/current/" },
    ],
  },
  ruby: {
    intro:
      "Ruby is a dynamic, expressive language designed for programmer happiness and famous as the foundation of Ruby on Rails. This track covers scripting fundamentals through blocks, modules and building a CLI.",
    lessonNotes: {
      "irb and your first script": "Experiment interactively in `irb` and run a `.rb` file with `ruby script.rb`.",
      "Variables": "Assign local variables without declaring types, and understand Ruby's dynamic typing.",
      "Strings and symbols": "Interpolate strings with `#{}` and distinguish mutable `String`s from immutable `:symbols`.",
      "Control flow": "Use `if`/`unless`/`case` and modifier-form conditionals like `puts x if x > 0`.",
      "Methods": "Define methods with `def`, default arguments, and implicit last-expression return values.",
      "Arrays": "Manipulate arrays with `push`, `map`, `select` and other Enumerable-backed methods.",
      "Hashes": "Create and query hashes with symbol or string keys using `{ key: value }` syntax.",
      "Blocks": "Pass a block to a method with `do...end` or `{ }` and yield to it with `yield`.",
      "Enumerable": "Use `Enumerable` methods like `map`, `select`, `reduce` and `each_with_index` on collections.",
      "Procs and lambdas": "Create reusable blocks of code with `Proc.new` and `->(x) { }` lambdas, noting their return semantics differ.",
      "Classes": "Define a class with `initialize`, instance variables (`@var`), and instance methods.",
      "Modules and mixins": "Share behavior across classes by defining a `module` and mixing it in with `include`.",
      "Attribute accessors": "Use `attr_accessor`/`attr_reader` to generate getter/setter methods for instance variables.",
      "Exceptions": "Raise and rescue errors with `raise`/`begin...rescue...ensure`.",
      "Metaprogramming intro": "Define methods dynamically at runtime using `define_method` and `method_missing`.",
      "Gems and Bundler": "Declare dependencies in a `Gemfile` and install them with `bundle install`.",
      "File I/O": "Read and write files using `File.read` and `File.open` with a block.",
      "RSpec testing": "Write `describe`/`it` blocks and assertions with `expect(...).to eq(...)` in RSpec.",
      "Rake tasks": "Define custom automation tasks in a `Rakefile` and run them with `rake task_name`.",
      "Building a CLI": "Parse command-line arguments with `ARGV` or `OptionParser` to build an interactive script.",
    },
    exercises: [
      { title: "Enumerable transform", prompt: "Given an array of numbers, use `map` and `select` to return only the squares of the even numbers.", hint: "`nums.select(&:even?).map { |n| n ** 2 }`" },
      { title: "Custom each with a block", prompt: "Write a method `each_pair` that yields consecutive pairs of an array to a block.", hint: "Iterate with `array.each_cons(2) { |a, b| yield a, b }`." },
      { title: "Module mixin", prompt: "Create a `Greetable` module with a `greet` method, and include it in two different classes.", hint: "`include Greetable` inside the class body makes the module's methods instance methods." },
      { title: "RSpec for a Stack class", prompt: "Write a `Stack` class with `push`/`pop`, and RSpec tests covering push, pop and popping an empty stack.", hint: "Use `expect { stack.pop }.to raise_error(...)` for the empty case." },
    ],
    projects: [
      { title: "Command-Line Journal", brief: "A CLI app for writing and listing dated journal entries stored in a text file.", steps: ["Parse subcommands from `ARGV` (add/list)", "Append entries with a timestamp to a file", "Read and format entries for the list command", "Handle missing file/edge cases gracefully", "Add RSpec tests for the entry formatting logic"], skills: ["File I/O", "CLI parsing", "Testing"] },
      { title: "Library Management Script", brief: "An object-oriented script modeling books, members and checkouts.", steps: ["Define `Book` and `Member` classes with attr_accessors", "Implement a `Library` class managing checkouts and returns", "Use a module to share logging behavior across classes", "Raise custom exceptions for invalid checkouts", "Cover the checkout logic with RSpec"], skills: ["OOP", "Modules", "Exceptions", "RSpec"] },
      { title: "Gem-based Task Runner", brief: "A small reusable gem-style project with Bundler and Rake automation.", steps: ["Set up a `Gemfile` and Bundler-managed dependencies", "Structure the code as a library with a public API", "Add Rake tasks for running tests and cleaning output", "Implement metaprogramming to dynamically register task handlers", "Write RSpec coverage for the public API"], skills: ["Gems and Bundler", "Rake", "Metaprogramming", "RSpec"] },
    ],
    resources: [
      { label: "Ruby Documentation", url: "https://www.ruby-lang.org/en/documentation/" },
      { label: "RSpec Documentation", url: "https://rspec.info/documentation/" },
      { label: "Bundler Guides", url: "https://bundler.io/guides/getting_started.html" },
    ],
  },
  kotlin: {
    intro:
      "Kotlin is a concise, null-safe language that runs on the JVM and is Google's official language for Android development. This track covers syntax and types through coroutines and a first Jetpack Compose screen.",
    lessonNotes: {
      "Setup": "Install Kotlin tooling and run a `.kt` file with `kotlinc` or via IntelliJ IDEA.",
      "val vs var": "Prefer immutable `val` bindings over mutable `var`, following Kotlin's idiomatic style.",
      "Null safety": "Distinguish nullable `String?` from non-null `String`, and use the safe-call operator `?.` and Elvis operator `?:`.",
      "Functions": "Define functions with default parameter values, named arguments, and single-expression syntax.",
      "Control flow": "Use `if`/`when` as expressions, and `for` loops over ranges and collections.",
      "Classes and data classes": "Define classes with primary constructors, and use `data class` for automatic `equals`/`hashCode`/`copy`.",
      "Sealed classes": "Model restricted class hierarchies with `sealed class` for exhaustive `when` handling.",
      "Extension functions": "Add new functions to existing types using `fun String.shout() = ...` syntax.",
      "Collections": "Use immutable `List`/`Map`/`Set` and their mutable counterparts, with functional operators like `filter`/`map`.",
      "Lambdas": "Pass lambda expressions to higher-order functions using trailing lambda syntax.",
      "Suspend functions": "Mark long-running functions with `suspend` so they can be paused and resumed by coroutines.",
      "Scopes": "Launch coroutines within a `CoroutineScope` using `launch` or `async`.",
      "Flows": "Emit and collect asynchronous streams of values with `Flow<T>` and `collect`.",
      "Error handling": "Handle coroutine exceptions with `try`/`catch` inside suspend functions and `CoroutineExceptionHandler`.",
      "Testing coroutines": "Test suspend functions using `runTest` from `kotlinx-coroutines-test`.",
      "Project structure": "Understand a typical Android project's module layout, manifest and Gradle build files.",
      "Activities": "Create an `Activity` (or entry composable) that serves as a screen's entry point.",
      "Jetpack Compose basics": "Build UI declaratively with `@Composable` functions like `Text` and `Column`.",
      "State": "Manage UI state in Compose using `remember { mutableStateOf(...) }`.",
      "Networking": "Fetch data from a network call inside a coroutine and update Compose state with the result.",
    },
    exercises: [
      { title: "Null-safe parser", prompt: "Write a function that safely parses a nullable string to an `Int?`, returning 0 if parsing fails.", hint: "Use `str?.toIntOrNull() ?: 0`." },
      { title: "Data class copy", prompt: "Define a `data class User(name: String, age: Int)` and create a modified copy with a different age using `.copy()`.", hint: "`val older = user.copy(age = user.age + 1)`" },
      { title: "Sealed class result", prompt: "Model a `Result` sealed class with `Success(data)` and `Failure(error)` variants, handled with an exhaustive `when`.", hint: "The compiler enforces exhaustiveness only if `Result` is `sealed`." },
      { title: "Coroutine delay test", prompt: "Write a suspend function that delays 1 second then returns a value, and test it with `runTest` without real delay.", hint: "`runTest` automatically skips virtual time for `delay()` calls." },
    ],
    projects: [
      { title: "Console Quiz App", brief: "A command-line quiz game using data classes and sealed classes for question types.", steps: ["Model questions with a sealed class hierarchy", "Load questions into a `List<Question>`", "Loop through questions and score answers", "Use extension functions for input parsing", "Add unit tests for scoring logic"], skills: ["Data classes", "Sealed classes", "Extension functions", "Testing"] },
      { title: "Async Weather Fetcher", brief: "A coroutine-based module that fetches weather data from an API concurrently for multiple cities.", steps: ["Define a suspend function to fetch data for one city", "Use `async`/`awaitAll` to fetch multiple cities concurrently", "Model responses with data classes", "Handle network errors with try/catch", "Test the fetch logic with `runTest`"], skills: ["Coroutines", "Suspend functions", "Error handling", "Testing"] },
      { title: "Compose Notes Screen", brief: "A single-screen Jetpack Compose app for adding and viewing notes.", steps: ["Set up a Compose project structure", "Build a `Column` with a `TextField` and a list of notes", "Manage note state with `remember { mutableStateOf(...) }`", "Persist notes by fetching/saving via a coroutine", "Add basic UI state handling for loading and empty states"], skills: ["Jetpack Compose", "State", "Coroutines", "Networking"] },
    ],
    resources: [
      { label: "Kotlin Documentation", url: "https://kotlinlang.org/docs/home.html" },
      { label: "Kotlin Coroutines Guide", url: "https://kotlinlang.org/docs/coroutines-guide.html" },
      { label: "Jetpack Compose Documentation", url: "https://developer.android.com/jetpack/compose/documentation" },
    ],
  },
  swift: {
    intro:
      "Swift is Apple's modern, type-safe language for building iOS, macOS and server-side applications. This track moves from playgrounds and optionals to protocols and a first SwiftUI screen.",
    lessonNotes: {
      "Playgrounds": "Experiment with Swift code interactively in an Xcode Playground with live results.",
      "Constants and variables": "Declare immutable values with `let` and mutable ones with `var`, relying on type inference.",
      "Optionals": "Represent absence of a value with `Int?`, and unwrap safely using `if let`, `guard let` or `??`.",
      "Functions": "Define functions with parameter labels, default values, and multiple return values via tuples.",
      "Control flow": "Use `if`/`switch` (with pattern matching) and `for-in` loops over ranges and collections.",
      "Structs and classes": "Compare value-type `struct` with reference-type `class`, and know when each is copied vs shared.",
      "Enums": "Define `enum` cases, including associated values, and switch over them exhaustively.",
      "Protocols": "Declare a `protocol` describing required methods/properties and conform to it from structs or classes.",
      "Extensions": "Add new functionality to existing types with `extension`, including protocol conformance.",
      "Generics": "Write generic functions and types constrained with `where` clauses or protocol bounds.",
      "Collections": "Use `Array`, `Dictionary` and `Set`, and transform them with `map`/`filter`/`reduce`.",
      "Codable and JSON": "Decode and encode JSON by conforming a type to `Codable` and using `JSONDecoder`/`JSONEncoder`.",
      "Error handling": "Throw and catch errors using `throws`, `try`, and `do { } catch { }`.",
      "Closures": "Pass closures as arguments using trailing closure syntax and capture variables from their surrounding scope.",
      "Concurrency": "Write asynchronous code with `async`/`await` and run tasks with `Task { }`.",
      "Views and modifiers": "Build UI with SwiftUI `View`s and chain modifiers like `.padding()` and `.foregroundColor()`.",
      "State and bindings": "Manage local view state with `@State` and pass mutable bindings with `@Binding`.",
      "Lists": "Render dynamic collections in SwiftUI with the `List` view and `ForEach`.",
      "Navigation": "Push and pop screens using `NavigationStack` and `NavigationLink`.",
      "Networking in SwiftUI": "Fetch data asynchronously in a view using `.task { }` combined with `async`/`await` networking calls.",
    },
    exercises: [
      { title: "Optional chaining", prompt: "Write a function that safely accesses a nested optional property and returns a default if any link is nil.", hint: "Chain `?.` calls and finish with `?? defaultValue`." },
      { title: "Enum with associated values", prompt: "Model a `NetworkResult` enum with `.success(String)` and `.failure(Error)` cases, and switch over it.", hint: "Use `case .success(let data):` to bind the associated value." },
      { title: "Codable model", prompt: "Define a `struct User: Codable` and decode a sample JSON string into an instance.", hint: "`JSONDecoder().decode(User.self, from: jsonData)`" },
      { title: "SwiftUI counter", prompt: "Build a small SwiftUI view with a `Text` showing a count and a `Button` that increments it.", hint: "Store the count in `@State private var count = 0`." },
    ],
    projects: [
      { title: "CLI Task List", brief: "A simple Swift command-line tool managing tasks with structs and enums.", steps: ["Define a `Task` struct and a `Priority` enum", "Store tasks in an array and support add/complete/list", "Persist tasks to a JSON file with Codable", "Handle errors gracefully with `do/catch`", "Add unit tests for the task logic"], skills: ["Structs", "Enums", "Codable", "Error handling"] },
      { title: "Weather Fetch Module", brief: "An async networking module that fetches and decodes weather data from a public API.", steps: ["Define Codable models matching the API response", "Write an async function using `URLSession` to fetch data", "Handle network and decoding errors with custom error types", "Cache the last successful result in memory", "Write tests using mock data"], skills: ["Concurrency", "Codable", "Error handling", "Networking"] },
      { title: "SwiftUI Notes App", brief: "A single-screen SwiftUI app for creating and viewing a list of notes.", steps: ["Build a `NavigationStack` with a list of notes", "Add a form screen to create a new note using `@State`", "Pass data between screens using `@Binding`", "Fetch and store notes asynchronously with `.task`", "Polish UI with view modifiers and navigation links"], skills: ["SwiftUI", "State and bindings", "Navigation", "Networking"] },
    ],
    resources: [
      { label: "Swift.org Documentation", url: "https://www.swift.org/documentation/" },
      { label: "The Swift Programming Language Book", url: "https://docs.swift.org/swift-book/" },
      { label: "Apple SwiftUI Tutorials", url: "https://developer.apple.com/tutorials/swiftui" },
    ],
  },
  react: {
    intro:
      "React is the most widely-used UI library for building component-driven, interactive web interfaces. This track covers JSX and components through hooks in depth and building tested, production-style apps.",
    lessonNotes: {
      "Why React": "Understand the problems React solves versus manual DOM manipulation, including declarative rendering and reusable components.",
      "JSX": "Write JSX syntax that compiles to `React.createElement` calls, embedding expressions with `{}`.",
      "Components and props": "Create function components and pass data into them via `props`.",
      "Rendering lists": "Render arrays of data with `.map()` and assign a unique `key` prop to each item.",
      "Conditional rendering": "Show or hide UI using `&&`, ternaries, or early returns inside a component.",
      "useState": "Manage local component state with the `useState` hook and trigger re-renders by calling its setter.",
      "Events": "Attach event handlers like `onClick`/`onChange` and access event data via the synthetic event object.",
      "useEffect": "Run side effects after render with `useEffect`, controlling execution with a dependency array.",
      "Forms": "Build controlled form inputs where value and `onChange` are tied to component state.",
      "Lifting state up": "Move shared state to a common parent component and pass it down via props to sibling components.",
      "useRef": "Access DOM nodes or persist mutable values across renders without triggering re-renders using `useRef`.",
      "useMemo and useCallback": "Memoize expensive computations with `useMemo` and stable function references with `useCallback`.",
      "Custom hooks": "Extract reusable stateful logic into a custom hook function prefixed with `use`.",
      "useContext": "Share state across the component tree without prop drilling using `createContext` and `useContext`.",
      "useReducer": "Manage complex state transitions with `useReducer` and a reducer function of `(state, action) => newState`.",
      "Routing": "Define client-side routes and navigation with `react-router-dom`'s `<Routes>`/`<Route>`.",
      "Data fetching": "Fetch data in a component using `useEffect` with `fetch`, handling loading and error states.",
      "Error boundaries": "Catch rendering errors in a subtree using a class component with `componentDidCatch` or a library helper.",
      "Performance": "Avoid unnecessary re-renders with `React.memo`, correct dependency arrays, and code-splitting via `lazy`/`Suspense`.",
      "Testing components": "Write component tests with React Testing Library, querying by role/text and firing events.",
    },
    exercises: [
      { title: "Toggle visibility", prompt: "Build a component with a button that toggles showing/hiding a paragraph of text using `useState`.", hint: "Store a boolean in state and render the text conditionally with `&&`." },
      { title: "Filtered list", prompt: "Render a list of items with a text input that filters the list as the user types.", hint: "Store the search term in state and use `.filter()` before mapping to `<li>`." },
      { title: "Custom useLocalStorage hook", prompt: "Write a custom hook `useLocalStorage(key, initialValue)` that syncs state with `localStorage`.", hint: "Use `useEffect` to write to `localStorage` whenever the state changes." },
      { title: "Fetch with loading state", prompt: "Fetch a list of posts from an API in `useEffect` and show a loading spinner until the data arrives.", hint: "Track a `loading` boolean in state, setting it false in the `.then()` or after `await`." },
    ],
    projects: [
      { title: "Interactive Quiz App", brief: "A multi-question quiz that tracks the current question, selected answers and a final score.", steps: ["Model questions as an array of objects", "Use `useState` to track current question index and score", "Render answer options and handle selection via events", "Show a results screen when the quiz ends", "Add React Testing Library tests for answer selection"], skills: ["useState", "Events", "Conditional rendering", "Testing"] },
      { title: "Task Board with Context", brief: "A Kanban-style board with tasks shared across components via context.", steps: ["Define a `TasksContext` with `useContext`", "Manage tasks state with `useReducer` for add/move/delete actions", "Build draggable or button-based columns for task status", "Persist tasks to `localStorage` with a custom hook", "Write tests for the reducer logic"], skills: ["useContext", "useReducer", "Custom hooks", "Testing"] },
      { title: "Multi-page Data Dashboard", brief: "A routed app that fetches and displays paginated data with error handling.", steps: ["Set up routes with `react-router-dom`", "Fetch data per route using `useEffect` with loading/error states", "Add pagination controls updating query state", "Wrap the dashboard route in an error boundary", "Optimize re-renders with `useMemo`/`React.memo` and add tests"], skills: ["Routing", "Data fetching", "Error boundaries", "Performance"] },
    ],
    resources: [
      { label: "React Documentation", url: "https://react.dev/learn" },
      { label: "React Router Documentation", url: "https://reactrouter.com/en/main" },
      { label: "React Testing Library Docs", url: "https://testing-library.com/docs/react-testing-library/intro/" },
    ],
  },
};

export default batch1;
export { batch1 };
