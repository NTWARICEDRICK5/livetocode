import type { CourseDetailMap } from "@/data/courseDetailTypes";

const batch2: CourseDetailMap = {
  nextjs: {
    intro:
      "Next.js is the production React framework that adds file-based routing, server rendering, and full-stack API capabilities on top of React. You'll learn to build multi-page apps that render on the server and ship fast.",
    lessonNotes: {
      "Project setup": "Scaffold a new app with `npx create-next-app@latest` and explore the generated `app/` directory structure.",
      "App router": "Learn how the `app/` directory maps folder structure to URL routes using `page.tsx` and `layout.tsx` files.",
      "Pages and layouts": "Create nested layouts with a shared `layout.tsx` that wraps child `page.tsx` routes and persists UI like navbars.",
      "Linking and navigation": "Use the `<Link>` component and `useRouter()` from `next/navigation` for client-side transitions without full reloads.",
      "Metadata": "Export a `metadata` object or `generateMetadata()` function from a page to control `<title>` and SEO tags.",
      "Server vs client components": "Distinguish default Server Components from `'use client'` components and know when each is required.",
      "Static generation": "Use `generateStaticParams()` to pre-render dynamic routes at build time for static site generation.",
      "Streaming": "Wrap slow-loading UI in `<Suspense>` boundaries so Next.js can stream HTML to the browser as data resolves.",
      "Caching": "Configure `fetch()` caching behavior with `{ cache: 'force-cache' }`, `revalidate`, and `no-store` options.",
      "Loading states": "Add a `loading.tsx` file next to a route to show an automatic fallback UI while the segment loads.",
      "Fetching in server components": "Call `fetch()` or a database client directly inside an `async` Server Component without extra state management.",
      "Route handlers": "Define API endpoints in `app/api/*/route.ts` exporting `GET`, `POST`, etc. functions.",
      "Server actions": "Mark a function `'use server'` and call it directly from a form's `action` prop to mutate data without a separate API call.",
      "Forms": "Wire a `<form>` to a server action and read submitted values via `FormData` inside the action.",
      "Validation": "Validate server action input with a schema library like Zod before writing to the database.",
      "Images and fonts": "Use `next/image` for automatic image optimization and `next/font` to self-host and load fonts without layout shift.",
      "SEO": "Generate sitemaps and robots files with `sitemap.ts` and `robots.ts` conventions.",
      "Middleware": "Write a `middleware.ts` file to intercept requests for redirects, auth checks, or rewriting paths before they hit a route.",
      "Auth patterns": "Implement session-based or JWT auth using cookies set from a route handler or server action.",
      "Deployment": "Deploy the app to Vercel or a Node server, understanding the difference between static and server-rendered output.",
    },
    exercises: [
      { title: "Dynamic route", prompt: "Create a route at `app/blog/[slug]/page.tsx` that reads `params.slug` and renders it.", hint: "The folder name `[slug]` becomes a dynamic segment available in the page's props." },
      { title: "Suspense fallback", prompt: "Wrap a component that fetches data in `<Suspense fallback={<p>Loading...</p>}>`.", hint: "Suspense requires the child to be an async Server Component or use a data-fetching library that supports it." },
      { title: "Route handler", prompt: "Add `app/api/hello/route.ts` exporting a `GET` function that returns `Response.json({ message: 'hi' })`.", hint: "Route handlers export HTTP-method-named async functions." },
      { title: "Server action form", prompt: "Build a form that calls a `'use server'` function to log the submitted text to the console.", hint: "Pass the server action directly as the form's `action` attribute." },
    ],
    projects: [
      {
        title: "Blog with static generation",
        brief: "Build a blog where posts are stored as markdown files and pre-rendered at build time.",
        steps: ["Set up `app/blog/[slug]/page.tsx`", "Read markdown files from a `content/` folder", "Use `generateStaticParams()` to list all slugs", "Render markdown to HTML with a library like `remark`", "Add metadata per post via `generateMetadata()`"],
        skills: ["Static generation", "Dynamic routes", "Metadata API"],
      },
      {
        title: "Full-stack task tracker",
        brief: "Create a task list app with server actions handling create, update, and delete against a database.",
        steps: ["Model tasks in a database (e.g. SQLite with Prisma)", "Build a server action to insert tasks", "Render tasks in a Server Component", "Add a server action to toggle completion", "Revalidate the path after each mutation"],
        skills: ["Server actions", "Server components", "Data mutations"],
      },
      {
        title: "Authenticated dashboard",
        brief: "Build a login-protected dashboard using middleware to guard routes.",
        steps: ["Create a login route handler that sets a session cookie", "Write `middleware.ts` to redirect unauthenticated users", "Build a protected `/dashboard` route", "Read the session in a Server Component to show the user's name", "Add a logout route that clears the cookie"],
        skills: ["Middleware", "Route handlers", "Auth patterns"],
      },
    ],
    resources: [
      { label: "Next.js Documentation", url: "https://nextjs.org/docs" },
      { label: "Next.js App Router Learn Course", url: "https://nextjs.org/learn" },
      { label: "Vercel Deployment Docs", url: "https://vercel.com/docs/frameworks/nextjs" },
    ],
  },

  vue: {
    intro:
      "Vue is a progressive JavaScript framework for building user interfaces with a gentle learning curve. You'll learn the Composition API, reactivity, and how to structure a full single-page application.",
    lessonNotes: {
      "Setup with Vite": "Scaffold a project with `npm create vue@latest`, which uses Vite for fast dev-server startup and HMR.",
      "Template syntax": "Use `{{ }}` interpolation and attribute bindings like `:href` inside a component's `<template>` block.",
      "Reactivity": "Create reactive state with `ref()` and `reactive()` from the Composition API and understand `.value` unwrapping.",
      "Directives": "Apply built-in directives such as `v-if`, `v-for`, and `v-bind` to control rendering declaratively.",
      "Events": "Listen to DOM events with `v-on` (`@click`) and emit custom events from child components with `defineEmits`.",
      "Props and emits": "Declare component inputs with `defineProps()` and communicate upward using `defineEmits()`.",
      "Slots": "Use `<slot>` to let parent components inject markup into a child, including named and scoped slots.",
      "Lifecycle": "Hook into component lifecycle with `onMounted`, `onUnmounted`, and `onUpdated` inside `<script setup>`.",
      "Composition API": "Organize logic with `<script setup>`, combining `ref`, `computed`, and `watch` in one cohesive function-based style.",
      "Composables": "Extract reusable reactive logic into a `useX()` function that returns refs and methods, shared across components.",
      "Vue Router": "Configure routes with `createRouter()` and `createWebHistory()`, and navigate using `<RouterLink>` and `useRouter()`.",
      "Pinia state": "Define a store with `defineStore()` holding state, getters, and actions, and access it via `useStore()` in components.",
      "Forms": "Bind form inputs with `v-model` for two-way data binding, including on custom components.",
      "Async data": "Fetch data inside `onMounted` or a composable using `fetch`, storing results in a `ref` and handling loading/error states.",
      "Transitions": "Wrap elements in `<Transition>` or `<TransitionGroup>` to animate enter/leave with CSS classes.",
      "Testing": "Write component tests with Vitest and `@vue/test-utils`, mounting components and asserting rendered output.",
      "Performance": "Use `v-memo`, `shallowRef`, and lazy-loaded routes to avoid unnecessary re-renders in large apps.",
      "TypeScript with Vue": "Type props and emits with `defineProps<Props>()` generics and enable strict checking via `vue-tsc`.",
      "SSR intro": "Understand how Nuxt builds on Vue to add server-side rendering and file-based routing.",
      "Deployment": "Build with `vite build` and deploy the static `dist/` output to any static host.",
    },
    exercises: [
      { title: "Counter with ref", prompt: "Create a component with a `count` ref and a button that increments it via `@click`.", hint: "Remember to access and mutate the value with `count.value++` inside `<script setup>`." },
      { title: "List rendering", prompt: "Render a list of items with `v-for` and give each `<li>` a `:key` bound to the item's id.", hint: "`v-for=\"item in items\" :key=\"item.id\"` is the standard pattern." },
      { title: "Custom event", prompt: "Build a child component that emits an `update` event with a payload when a button is clicked.", hint: "Use `defineEmits(['update'])` then call `emit('update', payload)`." },
      { title: "Pinia store", prompt: "Create a Pinia store with a `count` state and an `increment` action, and use it in two components.", hint: "`defineStore('counter', { state: () => ({ count: 0 }), actions: { increment() { this.count++ } } })`." },
    ],
    projects: [
      {
        title: "Todo app with Pinia",
        brief: "Build a todo list where state is managed centrally in a Pinia store.",
        steps: ["Scaffold the project with Vite", "Create a `useTodoStore` with add/remove/toggle actions", "Build a form component using `v-model`", "Render the list with `v-for` and conditional styling for completed items", "Persist the store to localStorage"],
        skills: ["Pinia", "v-model", "Composition API"],
      },
      {
        title: "Multi-page app with Vue Router",
        brief: "Create a small app with Home, About, and a dynamic detail page routed by id.",
        steps: ["Set up `createRouter` with three routes", "Add `<RouterLink>` navigation in the layout", "Build a dynamic route `/items/:id`", "Read the id with `useRoute()` and fetch matching data", "Add a route guard that redirects invalid ids"],
        skills: ["Vue Router", "Composables", "Dynamic routing"],
      },
      {
        title: "Reusable composable for data fetching",
        brief: "Extract a `useFetch(url)` composable used across multiple components.",
        steps: ["Create a composable returning `data`, `error`, and `loading` refs", "Trigger the fetch inside `onMounted`", "Use the composable in a list component", "Use it again in a detail component with a different URL", "Add basic error display when the fetch fails"],
        skills: ["Composables", "Reactivity", "Async data"],
      },
    ],
    resources: [
      { label: "Vue.js Guide", url: "https://vuejs.org/guide/introduction.html" },
      { label: "Vue Router Documentation", url: "https://router.vuejs.org/" },
      { label: "Pinia Documentation", url: "https://pinia.vuejs.org/" },
    ],
  },

  angular: {
    intro:
      "Angular is a complete, opinionated TypeScript framework used for large enterprise front-ends, built around components, dependency injection, and RxJS. You'll learn to structure and scale an Angular application.",
    lessonNotes: {
      "CLI and workspace": "Generate a new project with `ng new` and understand the workspace layout produced by the Angular CLI.",
      "Components": "Create a component with `ng generate component` and understand its decorator, template, and class structure.",
      "Templates": "Write Angular templates using interpolation `{{ }}` and structural directives like `*ngIf` and `*ngFor`.",
      "Data binding": "Use property binding `[value]`, event binding `(click)`, and two-way binding `[(ngModel)]`.",
      "Directives": "Apply attribute directives such as `ngClass` and `ngStyle`, and write a custom directive with `@Directive`.",
      "Services": "Create an injectable service with `@Injectable({ providedIn: 'root' })` to share logic across components.",
      "Dependency injection": "Inject a service into a component's constructor and understand Angular's hierarchical injector system.",
      "HttpClient": "Use `HttpClient.get()` from `@angular/common/http` to fetch data and subscribe to the returned Observable.",
      "Interceptors": "Write an `HttpInterceptor` to attach auth headers or handle errors globally for outgoing requests.",
      "Signals": "Use `signal()`, `computed()`, and `effect()` for Angular's newer fine-grained reactivity model.",
      "Observables": "Understand the Observable contract and use `.subscribe()`, `.pipe()`, and unsubscription patterns.",
      "RxJS operators": "Apply operators like `map`, `filter`, `switchMap`, and `debounceTime` to transform data streams.",
      "Forms": "Build forms with `ReactiveFormsModule` using `FormGroup` and `FormControl` for typed, testable forms.",
      "Validation": "Add built-in and custom validators to `FormControl` and display error messages conditionally.",
      "State patterns": "Manage shared state with a service holding a `BehaviorSubject` or with signals exposed as readonly.",
      "Routing and guards": "Configure routes with `RouterModule.forRoot()` and protect routes using a `CanActivate` guard.",
      "Lazy loading": "Split feature modules with `loadChildren` so routes are only downloaded when navigated to.",
      "Testing": "Write unit tests with Jasmine/Karma using `TestBed.configureTestingModule` to instantiate components.",
      "Performance": "Use `OnPush` change detection and `trackBy` in `*ngFor` to reduce unnecessary re-renders.",
      "Build and deploy": "Run `ng build --configuration production` and deploy the resulting static bundle.",
    },
    exercises: [
      { title: "Property binding", prompt: "Bind an `<img>` element's `[src]` to a component property called `imageUrl`.", hint: "Square brackets denote property binding: `[src]=\"imageUrl\"`." },
      { title: "Service injection", prompt: "Create a `LoggerService` and inject it into a component to log a message in `ngOnInit`.", hint: "Add the service to the constructor parameters: `constructor(private logger: LoggerService) {}`." },
      { title: "Reactive form", prompt: "Build a `FormGroup` with `name` and `email` controls, and disable the submit button when the form is invalid.", hint: "Use `[disabled]=\"form.invalid\"` on the submit button." },
      { title: "RxJS map operator", prompt: "Subscribe to an Observable of numbers and use `.pipe(map(n => n * 2))` before subscribing.", hint: "Import `map` from `rxjs/operators` and chain it inside `.pipe()`." },
    ],
    projects: [
      {
        title: "Contact list with services",
        brief: "Build a contact manager where a shared service holds and exposes the contact array.",
        steps: ["Generate a `ContactService` with an injectable array of contacts", "Create a `ContactListComponent` that displays them with `*ngFor`", "Add an `AddContactComponent` with a reactive form", "Emit new contacts through the service", "Add delete functionality with a button per row"],
        skills: ["Services", "Dependency injection", "Reactive forms"],
      },
      {
        title: "Routed feature app",
        brief: "Create an app with lazy-loaded feature modules and a route guard.",
        steps: ["Set up two feature modules with their own routing", "Configure `loadChildren` for lazy loading", "Add a `CanActivate` guard checking a mock login state", "Build a login page that sets the login state", "Verify navigation is blocked until logged in"],
        skills: ["Routing and guards", "Lazy loading", "Modules"],
      },
      {
        title: "Live search with RxJS",
        brief: "Build a search box that queries an API as the user types, debounced and cancel-safe.",
        steps: ["Bind an input to a `FormControl`", "Use `valueChanges.pipe(debounceTime(300), switchMap(...))`", "Call `HttpClient.get()` inside the `switchMap`", "Display results using `| async` in the template", "Handle empty and error states"],
        skills: ["RxJS operators", "HttpClient", "Reactive forms"],
      },
    ],
    resources: [
      { label: "Angular Documentation", url: "https://angular.dev/overview" },
      { label: "RxJS Documentation", url: "https://rxjs.dev/guide/overview" },
      { label: "Angular CLI Reference", url: "https://angular.dev/tools/cli" },
    ],
  },

  tailwind: {
    intro:
      "Tailwind CSS is a utility-first framework that lets you build interfaces by composing small, single-purpose classes directly in your markup. You'll learn to style, theme, and optimize a project without writing custom CSS files.",
    lessonNotes: {
      "Why utility-first": "Understand how composing utility classes like `flex`, `p-4`, and `text-lg` replaces writing bespoke CSS rules.",
      "Installation": "Install Tailwind via `npm install tailwindcss` and configure it with a `tailwind.config.js` and `@tailwind` directives.",
      "Core utilities": "Apply common utilities for text, background, and border, such as `text-red-500` and `bg-gray-100`.",
      "Spacing and sizing": "Use the spacing scale for `p-*`, `m-*`, `w-*`, and `h-*` classes based on Tailwind's default rem-based scale.",
      "Colors": "Use the built-in color palette with shade numbers (e.g. `blue-500`) and understand opacity modifiers like `bg-blue-500/50`.",
      "Flex utilities": "Build flexible layouts with `flex`, `justify-between`, `items-center`, and `gap-4`.",
      "Grid utilities": "Create grid layouts with `grid`, `grid-cols-3`, and `col-span-2`.",
      "Responsive prefixes": "Apply responsive variants like `md:flex-row` to change styles at breakpoints.",
      "Positioning": "Control layout with `relative`, `absolute`, `inset-0`, and `z-10`.",
      "Container queries": "Use the `@container` utilities to style elements based on their parent container's size rather than the viewport.",
      "Theme configuration": "Extend `theme.extend` in `tailwind.config.js` to add custom colors, fonts, and spacing values.",
      "Design tokens": "Define reusable design tokens as CSS variables referenced by custom Tailwind theme values.",
      "Dark mode": "Enable `darkMode: 'class'` and use `dark:` variants like `dark:bg-gray-900`.",
      "Typography plugin": "Install `@tailwindcss/typography` and apply the `prose` class to style rendered markdown content.",
      "Custom utilities": "Add new utility classes with the `@layer utilities` directive or the `addUtilities` plugin API.",
      "Component patterns": "Extract repeated utility combinations into reusable components or use `@apply` in a CSS file sparingly.",
      "Animations": "Use built-in `animate-spin`, `animate-pulse`, or define custom keyframes in the theme config.",
      "Forms": "Install `@tailwindcss/forms` to get consistent baseline styling for form elements.",
      "Accessibility": "Use `sr-only`, focus-visible utilities like `focus:ring-2`, and sufficient color contrast when styling.",
      "Optimizing output": "Rely on Tailwind's content scanning to purge unused classes so the production CSS bundle stays small.",
    },
    exercises: [
      { title: "Card layout", prompt: "Build a card with `p-6`, `rounded-lg`, `shadow-md`, and a `bg-white` background.", hint: "Combine spacing, border-radius, and shadow utilities on a single `<div>`." },
      { title: "Responsive nav", prompt: "Make a navbar that stacks vertically on mobile with `flex-col` and switches to `md:flex-row`.", hint: "Apply the mobile-first class first, then prefix the desktop override with `md:`." },
      { title: "Dark mode toggle", prompt: "Style a component to use `bg-white dark:bg-gray-800` and `text-black dark:text-white`.", hint: "Ensure `darkMode: 'class'` is set and toggle a `dark` class on `<html>`." },
      { title: "Custom color", prompt: "Add a custom `brand` color in `tailwind.config.js` and use it as `bg-brand`.", hint: "Extend `theme.extend.colors` with `brand: '#ff6600'`." },
    ],
    projects: [
      {
        title: "Landing page",
        brief: "Build a responsive marketing landing page using only Tailwind utilities.",
        steps: ["Create a hero section with `flex` and responsive text sizes", "Build a features grid with `grid-cols-1 md:grid-cols-3`", "Add a pricing card component with shadows and borders", "Add a footer with `flex justify-between`", "Test responsiveness at each breakpoint"],
        skills: ["Flex utilities", "Grid utilities", "Responsive prefixes"],
      },
      {
        title: "Themeable dashboard",
        brief: "Create a dashboard UI supporting light and dark mode with a custom theme.",
        steps: ["Configure `darkMode: 'class'` and custom brand colors", "Build a sidebar and header layout", "Add a dark mode toggle button that adds/removes the `dark` class", "Style cards and tables with `dark:` variants", "Verify contrast in both themes"],
        skills: ["Theme configuration", "Dark mode", "Design tokens"],
      },
      {
        title: "Blog with typography plugin",
        brief: "Render markdown blog posts styled cleanly with the typography plugin.",
        steps: ["Install `@tailwindcss/typography`", "Convert markdown to HTML with a library", "Wrap the output in a `prose` container", "Customize heading and link colors via typography theme options", "Add responsive `prose-lg` on larger screens"],
        skills: ["Typography plugin", "Theme configuration", "Responsive prefixes"],
      },
    ],
    resources: [
      { label: "Tailwind CSS Documentation", url: "https://tailwindcss.com/docs" },
      { label: "Tailwind Typography Plugin", url: "https://github.com/tailwindlabs/tailwindcss-typography" },
      { label: "Tailwind Forms Plugin", url: "https://github.com/tailwindlabs/tailwindcss-forms" },
    ],
  },

  nodejs: {
    intro:
      "Node.js lets you run JavaScript on the server to build fast, event-driven backends and APIs. You'll learn the runtime's core modules, Express, and how to ship a production-ready service.",
    lessonNotes: {
      "Runtime and event loop": "Understand how Node's single-threaded event loop handles I/O with callbacks, promises, and `libuv`.",
      "Modules": "Use CommonJS `require`/`module.exports` or ES module `import`/`export` syntax to split code into files.",
      "npm": "Initialize a project with `npm init`, install packages with `npm install`, and manage versions via `package.json`.",
      "File system": "Read and write files using the `fs` module, both callback-based and with `fs/promises`.",
      "Environment variables": "Load configuration from `process.env` and use a `.env` file with a package like `dotenv`.",
      "http module": "Create a raw server with `http.createServer((req, res) => {...})` before introducing a framework.",
      "Express routing": "Define routes with `app.get()`, `app.post()`, and route parameters like `/users/:id`.",
      "Middleware": "Write and use Express middleware functions `(req, res, next)` for logging, parsing, and auth checks.",
      "REST design": "Design resource-oriented endpoints following REST conventions (GET, POST, PUT, DELETE on `/resources`).",
      "Error handling": "Add a centralized Express error-handling middleware with four arguments `(err, req, res, next)`.",
      "Connecting a database": "Connect to a database like PostgreSQL or MongoDB using a driver or ORM such as `pg` or `mongoose`.",
      "Validation": "Validate incoming request bodies with a library like `zod` or `joi` before processing them.",
      "Sessions and JWT": "Implement stateless auth by signing and verifying JSON Web Tokens with the `jsonwebtoken` package.",
      "File uploads": "Handle multipart form uploads using `multer` middleware and store or process the uploaded files.",
      "Logging": "Add structured request logging with a library like `morgan` or `pino`.",
      "Testing": "Write API tests with `jest` or `vitest` combined with `supertest` to hit Express routes directly.",
      "Streams": "Use readable and writable streams to process large files without loading them fully into memory.",
      "Performance": "Profile and optimize with clustering (`cluster` module) or worker threads for CPU-bound work.",
      "Security headers": "Apply security middleware like `helmet` to set safe HTTP headers by default.",
      "Deployment": "Package the app for deployment with a `Procfile` or Dockerfile and manage the process with a tool like PM2.",
    },
    exercises: [
      { title: "Basic HTTP server", prompt: "Create a server with the `http` module that responds with 'Hello World' on `/`.", hint: "`http.createServer((req, res) => res.end('Hello World')).listen(3000)`." },
      { title: "Express route params", prompt: "Add an Express route `/users/:id` that returns the id as JSON.", hint: "Access it with `req.params.id` and respond with `res.json({ id })`." },
      { title: "Custom middleware", prompt: "Write a middleware function that logs the request method and URL, then calls `next()`.", hint: "Register it with `app.use((req, res, next) => {...})` before your routes." },
      { title: "JWT verification", prompt: "Write a middleware that verifies a Bearer token using `jsonwebtoken` and rejects invalid ones with 401.", hint: "Use `jwt.verify(token, secret)` inside a try/catch." },
    ],
    projects: [
      {
        title: "REST API for a notes app",
        brief: "Build a full CRUD REST API for notes backed by a database.",
        steps: ["Set up Express and a database connection", "Create routes for create/read/update/delete", "Add request validation with `zod`", "Add centralized error handling middleware", "Write tests with `supertest`"],
        skills: ["Express routing", "REST design", "Validation"],
      },
      {
        title: "Authenticated API",
        brief: "Add signup/login endpoints issuing JWTs, and protect other routes with an auth middleware.",
        steps: ["Create a `/signup` route hashing passwords with `bcrypt`", "Create a `/login` route issuing a signed JWT", "Write an auth middleware verifying the token", "Protect a `/profile` route with the middleware", "Handle expired/invalid token errors"],
        skills: ["Sessions and JWT", "Middleware", "Security headers"],
      },
      {
        title: "File upload service",
        brief: "Build an endpoint that accepts image uploads and streams them to disk.",
        steps: ["Set up `multer` for multipart parsing", "Validate file type and size", "Stream the file to a storage directory", "Return the stored file's URL", "Add a route to serve uploaded files statically"],
        skills: ["File uploads", "Streams", "Error handling"],
      },
    ],
    resources: [
      { label: "Node.js Documentation", url: "https://nodejs.org/en/docs" },
      { label: "Express Documentation", url: "https://expressjs.com/en/starter/installing.html" },
      { label: "npm Documentation", url: "https://docs.npmjs.com/" },
    ],
  },

  django: {
    intro:
      "Django is Python's batteries-included web framework, providing an ORM, admin site, templating, and authentication out of the box. You'll learn to build a full data-driven web app the Django way.",
    lessonNotes: {
      "Project and apps": "Create a project with `django-admin startproject` and add feature apps with `python manage.py startapp`.",
      "URLs and views": "Map URL patterns in `urls.py` to view functions or class-based views that return an `HttpResponse`.",
      "Templates": "Render HTML with Django's template language using `{{ variable }}` and `{% for %}` tags via `render()`.",
      "Static files": "Serve CSS/JS with the `static` template tag and configure `STATIC_URL`/`STATICFILES_DIRS`.",
      "Settings": "Configure `settings.py` for installed apps, middleware, database, and environment-specific values.",
      "ORM basics": "Define models as Python classes subclassing `models.Model` with fields like `CharField` and `ForeignKey`.",
      "Migrations": "Generate and apply schema changes with `python manage.py makemigrations` and `migrate`.",
      "Relationships": "Model one-to-many and many-to-many relations using `ForeignKey` and `ManyToManyField`.",
      "Querysets": "Query the database with the ORM's chainable API like `Model.objects.filter(...).order_by(...)`.",
      "Admin site": "Register models with `admin.site.register()` to manage data through Django's auto-generated admin UI.",
      "Forms": "Build forms with `forms.Form` or `forms.ModelForm` to validate and render input fields automatically.",
      "Validation": "Add field-level and form-level validation with `clean_<field>()` and `clean()` methods.",
      "User model": "Use Django's built-in `User` model or extend it with a custom user model for authentication.",
      "Login and permissions": "Protect views with `@login_required` and check permissions with `user.has_perm()`.",
      "Messages": "Use the `messages` framework to show one-time notifications like 'Saved successfully' after redirects.",
      "Django REST Framework intro": "Add DRF to expose model data as JSON APIs using `serializers.ModelSerializer` and `ViewSet`.",
      "Testing": "Write tests with Django's `TestCase`, using the test client to simulate requests against views.",
      "Caching": "Cache expensive views or querysets using Django's cache framework with `cache_page` or `cache.set/get`.",
      "Security checklist": "Run `python manage.py check --deploy` and review settings like `DEBUG`, `ALLOWED_HOSTS`, and CSRF protection.",
      "Deployment": "Serve the app with Gunicorn behind Nginx, and run `collectstatic` before going live.",
    },
    exercises: [
      { title: "Model and migration", prompt: "Define a `Post` model with `title` and `body` fields, then create and apply its migration.", hint: "Run `makemigrations` followed by `migrate` after defining the model." },
      { title: "Simple view", prompt: "Write a view function that returns a list of all `Post` objects rendered in a template.", hint: "Use `Post.objects.all()` and pass it to `render(request, 'template.html', {'posts': posts})`." },
      { title: "ModelForm", prompt: "Create a `PostForm` using `forms.ModelForm` and use it in a view to create new posts.", hint: "Set `class Meta: model = Post; fields = ['title', 'body']`." },
      { title: "Login required view", prompt: "Protect a view with `@login_required` so anonymous users are redirected to the login page.", hint: "Import from `django.contrib.auth.decorators` and decorate the view function." },
    ],
    projects: [
      {
        title: "Blog with admin management",
        brief: "Build a blog where posts are authored through the Django admin and displayed publicly.",
        steps: ["Define a `Post` model with author and timestamps", "Register it in `admin.py` with list display options", "Create public views listing and detailing posts", "Add templates extending a shared base layout", "Add pagination to the post list view"],
        skills: ["ORM basics", "Admin site", "Templates"],
      },
      {
        title: "Authenticated comment system",
        brief: "Add a comment feature where only logged-in users can post comments on blog posts.",
        steps: ["Create a `Comment` model with a `ForeignKey` to `Post` and `User`", "Build a `ModelForm` for submitting comments", "Protect the submit view with `@login_required`", "Display comments under each post", "Add success messages after posting"],
        skills: ["Relationships", "Forms", "Login and permissions"],
      },
      {
        title: "JSON API with DRF",
        brief: "Expose the blog's posts as a read/write JSON API using Django REST Framework.",
        steps: ["Install and configure `djangorestframework`", "Create a `PostSerializer`", "Build a `ModelViewSet` and register it with a router", "Add permission classes to restrict writes to authenticated users", "Write API tests using DRF's test client"],
        skills: ["Django REST Framework intro", "Querysets", "Testing"],
      },
    ],
    resources: [
      { label: "Django Documentation", url: "https://docs.djangoproject.com/en/stable/" },
      { label: "Django REST Framework Documentation", url: "https://www.django-rest-framework.org/" },
      { label: "Official Django Tutorial", url: "https://docs.djangoproject.com/en/stable/intro/tutorial01/" },
    ],
  },

  fastapi: {
    intro:
      "FastAPI is a modern, high-performance Python framework for building typed APIs with automatic interactive documentation. You'll learn Pydantic validation, async endpoints, and how to secure and ship an API.",
    lessonNotes: {
      "Setup and uvicorn": "Run a FastAPI app locally with `uvicorn main:app --reload` and inspect the auto-generated `/docs` page.",
      "Path and query params": "Declare typed path parameters like `/items/{item_id}: int` and optional query parameters with default values.",
      "Request bodies": "Define a Pydantic `BaseModel` and accept it as a typed request body parameter in a route function.",
      "Response models": "Use `response_model=` on a route decorator to control and validate the shape of returned data.",
      "Auto docs": "Explore the automatically generated OpenAPI schema and Swagger UI at `/docs` and ReDoc at `/redoc`.",
      "Pydantic models": "Model data with typed fields, defaults, and nested models using Pydantic's `BaseModel`.",
      "Custom validators": "Add field validation logic with Pydantic's `@field_validator` decorator.",
      "Error responses": "Raise `HTTPException(status_code=404, detail=...)` to return structured error responses.",
      "Dependencies": "Use `Depends()` to inject shared logic like database sessions or the current user into route functions.",
      "Settings": "Manage configuration with a `BaseSettings` class that reads environment variables typed and validated.",
      "SQLAlchemy integration": "Connect FastAPI to a database using SQLAlchemy models and a session dependency.",
      "Async database access": "Use an async database driver (e.g. `asyncpg` or `databases`) with `async def` route handlers for non-blocking queries.",
      "OAuth2 and JWT": "Implement `OAuth2PasswordBearer` for login flows and issue/verify JWTs for protected routes.",
      "Background tasks": "Run non-blocking follow-up work with `BackgroundTasks` injected into a route.",
      "File uploads": "Accept file uploads with `UploadFile` parameters and read their contents asynchronously.",
      "Testing with httpx": "Test endpoints using `httpx.AsyncClient` or FastAPI's `TestClient` against the app instance.",
      "CORS": "Add `CORSMiddleware` to allow requests from specific frontend origins.",
      "Middleware": "Write custom middleware with `@app.middleware('http')` to process requests/responses globally.",
      "Performance": "Understand how async endpoints and Uvicorn workers scale under concurrent load.",
      "Deployment": "Deploy with Uvicorn/Gunicorn workers behind a reverse proxy, or containerize with Docker.",
    },
    exercises: [
      { title: "Typed path param", prompt: "Create a route `/items/{item_id}` where `item_id` is typed as `int` and returned in the response.", hint: "FastAPI validates the type automatically based on the function signature annotation." },
      { title: "Pydantic request body", prompt: "Define an `Item` model with `name: str` and `price: float`, and accept it in a POST route.", hint: "Add the model as a parameter type; FastAPI parses and validates the JSON body." },
      { title: "HTTPException", prompt: "Raise a 404 `HTTPException` when a requested item id isn't found in a mock dictionary.", hint: "`raise HTTPException(status_code=404, detail='Item not found')`." },
      { title: "Dependency injection", prompt: "Write a dependency function returning a fake database session and inject it with `Depends()` into a route.", hint: "Define `def get_db(): ...` then use `db: Session = Depends(get_db)` as a parameter." },
    ],
    projects: [
      {
        title: "CRUD API with SQLAlchemy",
        brief: "Build a typed CRUD API for a resource backed by a SQLAlchemy database.",
        steps: ["Define SQLAlchemy models and Pydantic schemas", "Set up a database session dependency", "Implement create/read/update/delete routes", "Add response models to shape output", "Test endpoints with `TestClient`"],
        skills: ["SQLAlchemy integration", "Pydantic models", "Response models"],
      },
      {
        title: "JWT-authenticated API",
        brief: "Add login and protected endpoints secured with OAuth2 password flow and JWTs.",
        steps: ["Create a `/token` route using `OAuth2PasswordRequestForm`", "Hash and verify passwords with `passlib`", "Issue a JWT on successful login", "Write a dependency that decodes and validates the JWT", "Protect a route requiring the dependency"],
        skills: ["OAuth2 and JWT", "Dependencies", "Error responses"],
      },
      {
        title: "File processing API",
        brief: "Build an endpoint that accepts a CSV upload and processes it in the background.",
        steps: ["Accept a file with `UploadFile`", "Validate the file extension and size", "Schedule processing with `BackgroundTasks`", "Store a processing status in memory or a database", "Add a route to poll the processing status"],
        skills: ["File uploads", "Background tasks", "Async database access"],
      },
    ],
    resources: [
      { label: "FastAPI Documentation", url: "https://fastapi.tiangolo.com/" },
      { label: "Pydantic Documentation", url: "https://docs.pydantic.dev/latest/" },
      { label: "Uvicorn Documentation", url: "https://www.uvicorn.org/" },
    ],
  },

  laravel: {
    intro:
      "Laravel is a PHP framework known for elegant syntax, the Eloquent ORM, and a rich ecosystem covering routing, queues, and testing. You'll learn to build a full MVC application with database-backed models.",
    lessonNotes: {
      "Installation": "Create a new project with `laravel new` or `composer create-project laravel/laravel`, and run `php artisan serve`.",
      "Routing": "Define routes in `routes/web.php` using `Route::get()`, `Route::post()`, and route parameters like `{id}`.",
      "Controllers": "Generate a controller with `php artisan make:controller` and organize route logic into its methods.",
      "Blade templates": "Write views with Blade syntax like `@if`, `@foreach`, and `{{ $variable }}`, extending shared layouts with `@extends`.",
      "Artisan": "Use the `artisan` CLI for scaffolding, running migrations, and custom commands via `make:command`.",
      "Migrations": "Define schema changes in migration files using the `Schema::create()` builder and run them with `php artisan migrate`.",
      "Models": "Create Eloquent models with `php artisan make:model` mapping to database tables by convention.",
      "Relationships": "Define `hasMany`, `belongsTo`, and `belongsToMany` relationships as methods on Eloquent models.",
      "Seeding and factories": "Generate fake data with model factories and populate the database using seeders and `php artisan db:seed`.",
      "Query builder": "Use Eloquent's fluent query builder like `Model::where('status', 'active')->get()` for custom queries.",
      "Validation": "Validate incoming requests with `$request->validate([...])` or dedicated Form Request classes.",
      "Authentication": "Scaffold auth with Laravel Breeze or Fortify, using the `Auth` facade to log users in and out.",
      "Middleware": "Write middleware to intercept requests, such as checking authentication or roles, registered in `bootstrap/app.php` or `Kernel.php`.",
      "File storage": "Store uploaded files using the `Storage` facade with configurable disks like `local` or `s3`.",
      "Mail and notifications": "Send emails with Mailable classes and trigger them via `Mail::to($user)->send(new WelcomeMail())`.",
      "Queues and jobs": "Dispatch background jobs with `Job::dispatch()` and process them with `php artisan queue:work`.",
      "Events": "Fire events with `event(new OrderShipped($order))` and handle them in listeners registered in the event service provider.",
      "API resources": "Transform Eloquent models into JSON with `JsonResource` classes for consistent API responses.",
      "Testing": "Write feature and unit tests with PHPUnit or Pest, using Laravel's testing helpers like `$this->get()` and `assertStatus()`.",
      "Deployment": "Deploy with `php artisan optimize`, configure `.env` for production, and run migrations on release.",
    },
    exercises: [
      { title: "Basic route and controller", prompt: "Create a `/tasks` route pointing to a `TaskController@index` method that returns a Blade view.", hint: "Use `Route::get('/tasks', [TaskController::class, 'index'])`." },
      { title: "Eloquent relationship", prompt: "Define a `Task` model `belongsTo` a `User`, and a `User` model `hasMany` `Task`.", hint: "Add `public function user() { return $this->belongsTo(User::class); }`." },
      { title: "Form validation", prompt: "Validate a task creation request requiring `title` to be present and a string.", hint: "`$request->validate(['title' => 'required|string'])`." },
      { title: "Queued job", prompt: "Create a job class that sends a notification, and dispatch it instead of running it synchronously.", hint: "Generate with `php artisan make:job` and call `SendNotification::dispatch($user)`." },
    ],
    projects: [
      {
        title: "Task manager with auth",
        brief: "Build a task manager where each authenticated user only sees their own tasks.",
        steps: ["Scaffold auth with Laravel Breeze", "Create a `Task` model and migration linked to `User`", "Build a controller with index/store/update/destroy methods", "Scope queries to `auth()->user()->tasks`", "Style views with Blade layouts"],
        skills: ["Authentication", "Relationships", "Controllers"],
      },
      {
        title: "JSON API with resources",
        brief: "Expose the task manager's data as a JSON API using API resources.",
        steps: ["Define API routes in `routes/api.php`", "Create a `TaskResource` to shape JSON output", "Add form request validation for create/update", "Protect routes with Sanctum token authentication", "Write feature tests asserting JSON responses"],
        skills: ["API resources", "Validation", "Testing"],
      },
      {
        title: "Notification pipeline",
        brief: "Send a queued email notification whenever a task is marked complete.",
        steps: ["Fire a `TaskCompleted` event when a task updates", "Create a listener that queues a job", "Build a Mailable for the completion email", "Configure the queue driver and run `queue:work`", "Verify delivery in the local mail log (Mailtrap or `log` driver)"],
        skills: ["Events", "Queues and jobs", "Mail and notifications"],
      },
    ],
    resources: [
      { label: "Laravel Documentation", url: "https://laravel.com/docs" },
      { label: "Eloquent ORM Documentation", url: "https://laravel.com/docs/eloquent" },
      { label: "Laravel Bootcamp", url: "https://bootcamp.laravel.com/" },
    ],
  },

  "spring-boot": {
    intro:
      "Spring Boot is the industry-standard Java framework for building production-ready microservices and enterprise APIs with minimal configuration. You'll learn dependency injection, Spring Data JPA, and securing REST services.",
    lessonNotes: {
      "Spring Initializr": "Generate a starter project at start.spring.io choosing dependencies like Web, JPA, and Security.",
      "Beans and DI": "Define Spring beans with `@Component`/`@Service` and inject them via constructor injection.",
      "Configuration": "Externalize configuration using `application.properties` or `application.yml` files.",
      "Profiles": "Use `@Profile` and `application-{profile}.yml` to vary configuration between dev, test, and prod.",
      "Actuator": "Add `spring-boot-starter-actuator` to expose health and metrics endpoints like `/actuator/health`.",
      "REST controllers": "Build endpoints with `@RestController` and mapping annotations like `@GetMapping`.",
      "Request mapping": "Map path variables and query params using `@PathVariable` and `@RequestParam`.",
      "Validation": "Validate request bodies with `@Valid` and Bean Validation annotations like `@NotBlank` on DTOs.",
      "Exception handling": "Centralize error responses with `@ControllerAdvice` and `@ExceptionHandler` methods.",
      "DTOs": "Use dedicated data transfer objects to decouple API contracts from JPA entities.",
      "Spring Data JPA": "Define repositories by extending `JpaRepository<Entity, Id>` to get CRUD methods for free.",
      "Entities and relationships": "Annotate JPA entities with `@Entity`, `@Id`, and map relations with `@OneToMany`/`@ManyToOne`.",
      "Repositories": "Add derived query methods like `findByEmail(String email)` that Spring Data implements automatically.",
      "Transactions": "Mark service methods `@Transactional` to ensure multiple database operations commit or roll back together.",
      "Migrations with Flyway": "Version-control schema changes with SQL migration files under `db/migration` run automatically by Flyway.",
      "Spring Security": "Configure a `SecurityFilterChain` bean to define which endpoints require authentication.",
      "JWT auth": "Issue and validate JWTs in a custom filter added to the Spring Security filter chain.",
      "Testing": "Write tests with `@SpringBootTest` and `MockMvc` to exercise controllers against a test context.",
      "Observability": "Expose metrics to Prometheus/Micrometer and add structured logging for production monitoring.",
      "Docker deployment": "Package the app with a Dockerfile using a layered JAR and run it as a container.",
    },
    exercises: [
      { title: "REST endpoint", prompt: "Create a `@RestController` with a `GET /hello` endpoint returning a JSON message.", hint: "Annotate the method with `@GetMapping(\"/hello\")` and return a simple object or String." },
      { title: "JPA repository", prompt: "Define an `Employee` entity and a `EmployeeRepository extends JpaRepository<Employee, Long>`.", hint: "Spring Data JPA auto-implements `save`, `findAll`, and `findById` for you." },
      { title: "Validation annotation", prompt: "Add `@NotBlank` to a DTO's `name` field and use `@Valid` in the controller method to reject blank names.", hint: "Combine `@Valid @RequestBody EmployeeDto dto` in the controller signature." },
      { title: "Exception handler", prompt: "Write a `@ControllerAdvice` class handling `EntityNotFoundException` and returning a 404 response.", hint: "Use `@ExceptionHandler(EntityNotFoundException.class)` inside the advice class." },
    ],
    projects: [
      {
        title: "Employee management API",
        brief: "Build a CRUD REST API for managing employees backed by a relational database.",
        steps: ["Define the `Employee` entity and repository", "Create a `EmployeeController` with CRUD endpoints", "Add DTOs and validation for create/update requests", "Add a global exception handler for not-found errors", "Write `MockMvc` tests for each endpoint"],
        skills: ["Spring Data JPA", "REST controllers", "Validation"],
      },
      {
        title: "Secured API with JWT",
        brief: "Add authentication to the employee API so only logged-in users can modify data.",
        steps: ["Add Spring Security and configure a `SecurityFilterChain`", "Create a login endpoint issuing a JWT", "Write a JWT filter validating the token on each request", "Restrict write endpoints to authenticated users", "Test both authorized and unauthorized requests"],
        skills: ["Spring Security", "JWT auth", "Testing"],
      },
      {
        title: "Versioned schema with Flyway",
        brief: "Introduce Flyway migrations to manage schema evolution as the app grows.",
        steps: ["Add the Flyway dependency and configure the migration path", "Write an initial `V1__create_tables.sql` migration", "Add a `V2__add_column.sql` migration for a new feature", "Verify migrations run automatically on startup", "Add a repository method using the new column"],
        skills: ["Migrations with Flyway", "Entities and relationships", "Configuration"],
      },
    ],
    resources: [
      { label: "Spring Boot Documentation", url: "https://docs.spring.io/spring-boot/index.html" },
      { label: "Spring Data JPA Documentation", url: "https://docs.spring.io/spring-data/jpa/reference/index.html" },
      { label: "Spring Security Documentation", url: "https://docs.spring.io/spring-security/reference/index.html" },
    ],
  },
};

export default batch2;
export { batch2 };
