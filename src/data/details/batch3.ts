import type { CourseDetailMap } from "@/data/courseDetailTypes";

const batch3: CourseDetailMap = {
  sql: {
    intro:
      "SQL is the declarative language for querying and manipulating relational data. You'll learn to read and write queries that filter, join, and aggregate data stored in tables.",
    lessonNotes: {
      "SELECT and FROM": "Write your first SELECT statement to pull specific columns from a table, e.g. `SELECT name, price FROM products;`.",
      "WHERE filters": "Filter rows using `WHERE` with comparison operators like `=`, `>`, `<>`, and `BETWEEN`.",
      "ORDER BY and LIMIT": "Sort results with `ORDER BY column DESC` and cap row counts with `LIMIT 10`.",
      "Operators": "Combine conditions with `AND`, `OR`, `NOT`, `IN (...)`, and `LIKE '%pattern%'`.",
      "NULL handling": "Understand three-valued logic and use `IS NULL` / `IS NOT NULL` instead of `= NULL`.",
      "INNER JOIN": "Combine matching rows from two tables with `INNER JOIN table2 ON table1.id = table2.fk_id`.",
      "LEFT and RIGHT JOIN": "Keep unmatched rows from one side using `LEFT JOIN` to find records with no related match.",
      "UNION": "Stack result sets from multiple queries with `UNION` (dedupes) and `UNION ALL` (keeps duplicates).",
      "Subqueries": "Nest a query inside `WHERE` or `FROM`, e.g. `WHERE id IN (SELECT id FROM ...)`.",
      "CTEs": "Use `WITH cte_name AS (...)` to name a subquery and reference it for readability and recursion.",
      "COUNT, SUM, AVG": "Aggregate values across rows using `COUNT(*)`, `SUM(column)`, and `AVG(column)`.",
      "GROUP BY": "Collapse rows into groups with `GROUP BY category` before applying aggregate functions.",
      "HAVING": "Filter aggregated groups after `GROUP BY` using `HAVING COUNT(*) > 5`, unlike `WHERE` which filters raw rows.",
      "Window functions": "Compute running totals or rankings without collapsing rows using `OVER (PARTITION BY ... ORDER BY ...)`.",
      "Ranking": "Rank rows within groups using `RANK()`, `DENSE_RANK()`, and `ROW_NUMBER()`.",
      "Tables and types": "Create tables with `CREATE TABLE` and choose column types like `INTEGER`, `VARCHAR(255)`, `DATE`, and `BOOLEAN`.",
      "Primary and foreign keys": "Enforce uniqueness with `PRIMARY KEY` and reference other tables with `FOREIGN KEY ... REFERENCES`.",
      "Normalization": "Split repeating data into related tables following 1NF, 2NF, and 3NF to reduce redundancy.",
      "Indexes": "Speed up lookups by creating `CREATE INDEX idx_name ON table(column)` on frequently filtered columns.",
      "Transactions": "Wrap multiple statements in `BEGIN`, `COMMIT`, and `ROLLBACK` to ensure atomic changes.",
    },
    exercises: [
      { title: "Filter and sort products", prompt: "Write a query that selects all products priced above $20, ordered by price descending.", hint: "Use WHERE price > 20 and ORDER BY price DESC." },
      { title: "Join orders and customers", prompt: "List each order with the customer's name using an INNER JOIN.", hint: "Join on orders.customer_id = customers.id." },
      { title: "Group sales by category", prompt: "Show total revenue per product category, only for categories with revenue over 1000.", hint: "Use GROUP BY category with HAVING SUM(revenue) > 1000." },
      { title: "Rank top spenders", prompt: "Rank customers by total spend using a window function.", hint: "Use RANK() OVER (ORDER BY total_spend DESC)." },
    ],
    projects: [
      { title: "Sales Reporting Queries", brief: "Build a set of SQL queries answering common business questions from a sales dataset.", steps: ["Design or import a schema with orders, customers, products", "Write queries for top products, monthly revenue, and repeat customers", "Add CTEs to simplify multi-step calculations", "Optimize slow queries with indexes"], skills: ["Joins", "Aggregation", "CTEs", "Indexing"] },
      { title: "Library Database Schema", brief: "Design a normalized schema for a library system and populate it with sample data.", steps: ["Model books, authors, members, and loans", "Add primary and foreign keys", "Write queries to find overdue loans", "Add constraints to prevent invalid data"], skills: ["Schema design", "Normalization", "Constraints"] },
      { title: "Analytics Dashboard Backend", brief: "Write queries that power a small analytics dashboard using window functions.", steps: ["Compute running totals of daily signups", "Rank products by sales within each region", "Build a query returning month-over-month growth", "Document each query's purpose"], skills: ["Window functions", "Aggregation", "Query design"] },
    ],
    resources: [
      { label: "PostgreSQL SELECT Documentation", url: "https://www.postgresql.org/docs/current/sql-select.html" },
      { label: "SQLite SQL Syntax Reference", url: "https://www.sqlite.org/lang.html" },
      { label: "MDN: SQL Overview", url: "https://developer.mozilla.org/en-US/docs/Glossary/SQL" },
    ],
  },
  postgresql: {
    intro:
      "PostgreSQL is a powerful open-source relational database known for standards compliance, extensibility, and advanced features like JSONB and full-text search.",
    lessonNotes: {
      "Installation and psql": "Install PostgreSQL locally and connect using the `psql` command-line client with `psql -U user -d dbname`.",
      "Data types": "Choose from Postgres-specific types like `SERIAL`, `NUMERIC`, `TEXT`, `TIMESTAMPTZ`, and `UUID`.",
      "Schemas": "Organize objects into namespaces with `CREATE SCHEMA` and qualify tables as `schema.table`.",
      "Constraints": "Enforce data integrity with `CHECK`, `UNIQUE`, `NOT NULL`, and `EXCLUDE` constraints.",
      "Roles": "Manage access with `CREATE ROLE`, `GRANT`, and `REVOKE` to control table and database permissions.",
      "JSONB": "Store and query semi-structured data using the `jsonb` type with operators like `->`, `->>`, and `@>`.",
      "Arrays": "Store multiple values in one column using array types, e.g. `INTEGER[]`, and query with `ANY()`.",
      "Full-text search": "Search text efficiently using `to_tsvector` and `to_tsquery` with a GIN index.",
      "Views and materialized views": "Save reusable queries as `CREATE VIEW`, and precompute expensive ones with `CREATE MATERIALIZED VIEW`.",
      "Extensions": "Add functionality like UUID generation or geospatial support with `CREATE EXTENSION postgis;`.",
      "EXPLAIN ANALYZE": "Diagnose query performance by running `EXPLAIN ANALYZE` to see the actual execution plan and timings.",
      "Index types": "Choose the right index type: B-tree for equality/range, GIN for JSONB/arrays, GiST for geometric data.",
      "Query tuning": "Rewrite slow queries by avoiding unnecessary casts, adding covering indexes, and reducing row scans.",
      "Partitioning": "Split large tables into partitions with `PARTITION BY RANGE` or `LIST` to improve query performance.",
      "Vacuum": "Understand how `VACUUM` and `autovacuum` reclaim dead tuple space and prevent transaction ID wraparound.",
      "Backups": "Create backups with `pg_dump` and restore them using `pg_restore` or `psql`.",
      "Replication basics": "Set up streaming replication using a primary and standby server with `pg_basebackup`.",
      "Row-level security": "Restrict which rows a user can see with `CREATE POLICY` under `ROW LEVEL SECURITY`.",
      "Migrations": "Manage schema changes over time with versioned migration files and tools like `sqitch` or `Flyway`.",
      "Monitoring": "Track database health using `pg_stat_activity`, `pg_stat_statements`, and slow query logs.",
    },
    exercises: [
      { title: "Query JSONB data", prompt: "Given a table with a jsonb column of user preferences, write a query filtering rows where a nested key equals a value.", hint: "Use the ->> operator: WHERE data->>'theme' = 'dark'." },
      { title: "Read an execution plan", prompt: "Run EXPLAIN ANALYZE on a slow query and identify whether it uses a sequential scan or index scan.", hint: "Look for 'Seq Scan' vs 'Index Scan' in the output." },
      { title: "Create a materialized view", prompt: "Build a materialized view summarizing daily order totals and refresh it.", hint: "Use CREATE MATERIALIZED VIEW ... AS SELECT ... then REFRESH MATERIALIZED VIEW." },
      { title: "Set up row-level security", prompt: "Restrict a table so users can only see their own rows.", hint: "Enable ROW LEVEL SECURITY and CREATE POLICY with USING (user_id = current_user_id())." },
    ],
    projects: [
      { title: "JSONB Product Catalog", brief: "Build a product catalog that stores flexible attributes as JSONB and supports fast filtering.", steps: ["Design a table with core columns plus a jsonb attributes field", "Add a GIN index on the jsonb column", "Write queries filtering by nested attributes", "Benchmark with and without the index"], skills: ["JSONB", "Indexing", "Query tuning"] },
      { title: "Full-Text Search Engine", brief: "Add search to an articles table using Postgres full-text search.", steps: ["Add a tsvector column", "Populate it with to_tsvector from title and body", "Create a GIN index on the tsvector column", "Query with to_tsquery and rank results"], skills: ["Full-text search", "Indexes", "Views"] },
      { title: "Backup and Replication Lab", brief: "Practice backing up a database and setting up a read replica.", steps: ["Create a sample database with data", "Back it up with pg_dump", "Restore it into a fresh database", "Configure streaming replication to a standby"], skills: ["Backups", "Replication", "Operations"] },
    ],
    resources: [
      { label: "PostgreSQL Official Documentation", url: "https://www.postgresql.org/docs/current/" },
      { label: "PostgreSQL JSON Types", url: "https://www.postgresql.org/docs/current/datatype-json.html" },
      { label: "PostgreSQL Performance Tips (EXPLAIN)", url: "https://www.postgresql.org/docs/current/using-explain.html" },
    ],
  },
  mysql: {
    intro:
      "MySQL is the world's most widely deployed open-source relational database, powering countless web applications with fast reads and a mature ecosystem.",
    lessonNotes: {
      "Installing MySQL": "Install the MySQL server locally or via Docker and start the service with `mysqld` or `systemctl start mysql`.",
      "Clients and Workbench": "Connect using the `mysql` CLI client or the graphical MySQL Workbench tool.",
      "Databases and tables": "Create databases with `CREATE DATABASE shop;` and tables with `CREATE TABLE`.",
      "Data types": "Pick appropriate MySQL types like `INT`, `VARCHAR`, `DECIMAL`, `DATETIME`, and `ENUM`.",
      "Users and grants": "Create users with `CREATE USER` and control access with `GRANT SELECT, INSERT ON db.* TO user`.",
      "CRUD statements": "Practice `INSERT INTO`, `SELECT`, `UPDATE ... SET`, and `DELETE FROM` statements.",
      "Joins": "Combine tables with `INNER JOIN` and `LEFT JOIN` using matching key columns.",
      "Aggregation": "Summarize data using `COUNT`, `SUM`, `AVG` combined with `GROUP BY`.",
      "Stored procedures": "Encapsulate logic in the database with `CREATE PROCEDURE` and call it via `CALL proc_name()`.",
      "Triggers": "Automatically run logic on data changes using `CREATE TRIGGER ... BEFORE INSERT`.",
      "Indexes": "Speed up lookups with `CREATE INDEX idx_name ON table(column)` and understand composite indexes.",
      "EXPLAIN": "Analyze query plans with `EXPLAIN SELECT ...` to check whether indexes are being used.",
      "Query cache patterns": "Understand why MySQL removed the query cache and how to cache results at the application layer instead.",
      "Storage engines": "Compare InnoDB (transactional, row-locking) versus MyISAM (no transactions) storage engines.",
      "Schema tuning": "Normalize tables appropriately and choose correct column widths to reduce storage and improve speed.",
      "Backups with mysqldump": "Export a database with `mysqldump -u user -p dbname > backup.sql` and restore with `mysql < backup.sql`.",
      "Replication": "Set up primary-replica replication using binary logs and `CHANGE MASTER TO`.",
      "Security hardening": "Run `mysql_secure_installation`, disable remote root login, and use least-privilege grants.",
      "Monitoring": "Track performance using `SHOW PROCESSLIST`, `SHOW STATUS`, and the Performance Schema.",
      "Upgrades": "Plan version upgrades using `mysql_upgrade` and by testing against a staging replica first.",
    },
    exercises: [
      { title: "Set up a users table", prompt: "Create a users table with an auto-incrementing id, email (unique), and created_at columns.", hint: "Use INT AUTO_INCREMENT PRIMARY KEY and UNIQUE on email." },
      { title: "Write a stored procedure", prompt: "Create a stored procedure that returns all orders for a given customer id.", hint: "Use CREATE PROCEDURE get_orders(IN cust_id INT) and a SELECT inside it." },
      { title: "Analyze a query plan", prompt: "Run EXPLAIN on a query filtering by a non-indexed column, then add an index and compare.", hint: "Check the 'type' and 'key' columns in EXPLAIN output before and after indexing." },
      { title: "Back up and restore", prompt: "Dump a database to a file and restore it into a new database.", hint: "Use mysqldump for export and mysql for import." },
    ],
    projects: [
      { title: "Inventory Management Schema", brief: "Design and build a MySQL schema for tracking inventory across warehouses.", steps: ["Create tables for products, warehouses, and stock levels", "Add foreign keys and indexes", "Write a stored procedure to transfer stock between warehouses", "Add a trigger to log stock changes"], skills: ["Schema design", "Stored procedures", "Triggers"] },
      { title: "Query Performance Audit", brief: "Take a slow-running MySQL application query set and optimize it.", steps: ["Identify slow queries with the slow query log", "Run EXPLAIN on each", "Add appropriate indexes", "Re-measure and document improvements"], skills: ["EXPLAIN", "Indexes", "Query tuning"] },
      { title: "Backup and Replication Setup", brief: "Configure a primary-replica MySQL setup with automated backups.", steps: ["Enable binary logging on the primary", "Configure a replica with CHANGE MASTER TO", "Verify replication status", "Schedule nightly mysqldump backups"], skills: ["Replication", "Backups", "Operations"] },
    ],
    resources: [
      { label: "MySQL Reference Manual", url: "https://dev.mysql.com/doc/refman/8.0/en/" },
      { label: "MySQL mysqldump Documentation", url: "https://dev.mysql.com/doc/refman/8.0/en/mysqldump.html" },
      { label: "MySQL EXPLAIN Output Format", url: "https://dev.mysql.com/doc/refman/8.0/en/explain-output.html" },
    ],
  },
  mongodb: {
    intro:
      "MongoDB is a document database that stores data as flexible, JSON-like BSON documents, well suited to rapidly evolving application schemas.",
    lessonNotes: {
      "Databases and collections": "Understand how MongoDB organizes documents into collections within databases, created lazily on first insert.",
      "Insert and find": "Insert documents with `db.collection.insertOne()` and retrieve them with `db.collection.find()`.",
      "Update and delete": "Modify documents with `updateOne()`/`updateMany()` using `$set`, and remove with `deleteOne()`.",
      "Data types": "Use BSON types including `ObjectId`, `Date`, `Decimal128`, arrays, and nested objects.",
      "Schema design": "Decide between embedding related data in one document versus referencing another collection.",
      "Query operators": "Filter documents using operators like `$gt`, `$in`, `$exists`, and `$regex`.",
      "Projection": "Return only needed fields by passing a projection object as the second argument to `find()`.",
      "Sorting and paging": "Sort with `.sort({field: 1})` and paginate using `.skip()` and `.limit()`.",
      "Embedded documents": "Query nested fields with dot notation, e.g. `find({'address.city': 'Berlin'})`.",
      "Arrays": "Query and update array fields using `$elemMatch`, `$push`, and `$pull`.",
      "Pipeline stages": "Build aggregation pipelines from stages like `$match`, `$project`, and `$sort` in sequence.",
      "$group and $match": "Filter documents with `$match` and compute aggregates per group with `$group`.",
      "$lookup": "Join data from another collection using `$lookup`, MongoDB's aggregation-stage join.",
      "Faceted search": "Compute multiple aggregations at once using `$facet` for search result sidebars.",
      "Performance tips": "Use `.explain()` to check whether an aggregation or query uses an index efficiently.",
      "Indexes": "Create indexes with `db.collection.createIndex({field: 1})` to speed up queries and enforce uniqueness.",
      "Transactions": "Run multi-document ACID transactions using a session with `startTransaction()`/`commitTransaction()`.",
      "Replica sets": "Understand how replica sets provide high availability with automatic primary election.",
      "Atlas basics": "Deploy a managed cluster on MongoDB Atlas and connect using a connection string.",
      "Security": "Enable authentication, create roles with `db.createUser()`, and restrict network access.",
    },
    exercises: [
      { title: "Insert and query documents", prompt: "Insert three user documents and write a query to find users older than 25.", hint: "Use insertMany() then find({age: {$gt: 25}})." },
      { title: "Update nested fields", prompt: "Update a user's embedded address.city field without touching other fields.", hint: "Use updateOne with $set: {'address.city': 'Paris'}." },
      { title: "Aggregate orders by status", prompt: "Write an aggregation pipeline that groups orders by status and sums their totals.", hint: "Use $group with _id: '$status' and $sum: '$total'." },
      { title: "Join with $lookup", prompt: "Join an orders collection with a customers collection to include customer name in results.", hint: "Use $lookup with localField/foreignField matching customer_id." },
    ],
    projects: [
      { title: "Blog Platform Data Model", brief: "Design a MongoDB schema for a blog with posts, comments, and authors.", steps: ["Decide which data to embed vs reference", "Insert sample posts with embedded comments", "Write queries for posts by an author", "Add indexes for common queries"], skills: ["Schema design", "Embedding", "Indexing"] },
      { title: "Sales Analytics Pipeline", brief: "Build an aggregation pipeline that reports sales trends from an orders collection.", steps: ["Match orders within a date range", "Group by month and product category", "Use $lookup to enrich with product names", "Sort and limit to top categories"], skills: ["Aggregation", "$lookup", "Pipelines"] },
      { title: "User Auth with Atlas", brief: "Deploy a small user database on MongoDB Atlas with secure access.", steps: ["Create a free Atlas cluster", "Create a database user with a scoped role", "Connect from a script using the connection string", "Add a unique index on email"], skills: ["Atlas", "Security", "Indexes"] },
    ],
    resources: [
      { label: "MongoDB Manual", url: "https://www.mongodb.com/docs/manual/" },
      { label: "MongoDB Aggregation Pipeline Reference", url: "https://www.mongodb.com/docs/manual/core/aggregation-pipeline/" },
      { label: "MongoDB Atlas Documentation", url: "https://www.mongodb.com/docs/atlas/" },
    ],
  },
  redis: {
    intro:
      "Redis is an in-memory data store used for caching, session storage, queues, and real-time features, valued for its speed and rich data structures.",
    lessonNotes: {
      "Installation and redis-cli": "Install Redis and interact with it using the `redis-cli` interactive shell.",
      "Keys and expiry": "Set keys with `SET key value` and add expiration with `EXPIRE key 60` or `SET key value EX 60`.",
      "Strings": "Use string commands like `INCR`, `APPEND`, and `GETSET` for counters and simple values.",
      "Hashes": "Store object-like data with `HSET key field value` and read with `HGETALL key`.",
      "Lists": "Use `LPUSH`/`RPUSH` and `LPOP`/`RPOP` to build simple queues and stacks.",
      "Sets and sorted sets": "Store unique members with `SADD`, and ranked members with `ZADD key score member`.",
      "Streams": "Append event-log-style entries with `XADD` and consume them with `XREAD` for streaming pipelines.",
      "Bitmaps": "Track boolean flags per-bit efficiently with `SETBIT` and `BITCOUNT`.",
      "HyperLogLog": "Estimate cardinality of huge sets with constant memory using `PFADD` and `PFCOUNT`.",
      "Geospatial": "Store coordinates with `GEOADD` and query nearby points with `GEOSEARCH`.",
      "Cache-aside": "Implement the cache-aside pattern: check Redis first, fall back to the database, then populate the cache.",
      "Rate limiting": "Build a rate limiter using `INCR` with `EXPIRE` or the sliding-window sorted-set technique.",
      "Session storage": "Store session data as hashes keyed by session id with a TTL matching session expiry.",
      "Pub/Sub": "Publish and subscribe to channels with `PUBLISH channel message` and `SUBSCRIBE channel`.",
      "Distributed locks": "Implement a simple distributed lock using `SET key value NX PX ttl`.",
      "Persistence": "Understand RDB snapshotting versus AOF (append-only file) durability tradeoffs.",
      "Eviction policies": "Configure `maxmemory-policy` such as `allkeys-lru` to control behavior when memory is full.",
      "Clustering": "Understand how Redis Cluster shards keys across nodes using hash slots.",
      "Monitoring": "Use `INFO`, `MONITOR`, and `SLOWLOG` to observe Redis health and slow commands.",
      "Security": "Enable `requirepass`, use ACLs to restrict commands, and avoid exposing Redis publicly.",
    },
    exercises: [
      { title: "Build a counter", prompt: "Use Redis to track page views for a URL, incrementing on each visit.", hint: "Use INCR pageviews:url_id." },
      { title: "Cache a database query", prompt: "Implement cache-aside logic: check Redis, and if missing, fetch from a data source and store it with a TTL.", hint: "Use GET first; on miss, SET key value EX 300." },
      { title: "Rate limit requests", prompt: "Limit a user to 5 requests per minute using Redis.", hint: "Use INCR on a per-user-per-minute key with EXPIRE 60 set on first increment." },
      { title: "Implement pub/sub notifications", prompt: "Publish a message when an order is placed and have a subscriber log it.", hint: "Use PUBLISH orders '...' and SUBSCRIBE orders in another client." },
    ],
    projects: [
      { title: "API Response Cache", brief: "Add a Redis caching layer in front of a slow API endpoint.", steps: ["Identify a slow, frequently-called endpoint", "Cache its response in Redis with a TTL", "Invalidate the cache on relevant writes", "Measure latency improvement"], skills: ["Cache-aside", "TTL", "Performance"] },
      { title: "Leaderboard Service", brief: "Build a real-time game leaderboard using Redis sorted sets.", steps: ["Store player scores with ZADD", "Fetch top players with ZREVRANGE", "Get a player's rank with ZRANK", "Add real-time updates via Pub/Sub"], skills: ["Sorted sets", "Pub/Sub", "Real-time data"] },
      { title: "Distributed Rate Limiter", brief: "Build a reusable rate-limiting middleware backed by Redis.", steps: ["Design a key scheme per user and time window", "Implement the increment-and-expire logic", "Handle race conditions atomically", "Test under concurrent load"], skills: ["Rate limiting", "Atomic operations", "Middleware design"] },
    ],
    resources: [
      { label: "Redis Documentation", url: "https://redis.io/docs/latest/" },
      { label: "Redis Commands Reference", url: "https://redis.io/docs/latest/commands/" },
      { label: "Redis University", url: "https://redis.io/learn/university" },
    ],
  },
  git: {
    intro:
      "Git is the distributed version control system nearly every developer uses to track changes, collaborate, and safely experiment with code.",
    lessonNotes: {
      "Repositories": "Initialize a new repository with `git init` or clone an existing one with `git clone <url>`.",
      "Staging and commits": "Stage changes with `git add` and record them with `git commit -m \"message\"`.",
      "Status and log": "Check pending changes with `git status` and review history with `git log --oneline`.",
      "Ignoring files": "Exclude files from tracking by listing patterns in a `.gitignore` file.",
      "Diffs": "See exactly what changed using `git diff` for unstaged changes and `git diff --staged` for staged ones.",
      "Creating branches": "Create and switch branches with `git branch feature-x` and `git checkout feature-x` or `git switch -c feature-x`.",
      "Merging": "Combine branch histories with `git merge branch-name`, producing a merge commit when needed.",
      "Conflicts": "Resolve merge conflicts by editing the conflict markers in files, then `git add` and `git commit`.",
      "Rebasing": "Replay commits onto a new base with `git rebase main` for a linear history.",
      "Stashing": "Temporarily shelve uncommitted changes with `git stash` and restore them with `git stash pop`.",
      "Clone, push, pull": "Sync with a remote using `git clone`, `git push origin branch`, and `git pull`.",
      "Tracking branches": "Link a local branch to a remote one with `git push -u origin branch` so future pushes need no arguments.",
      "Tags": "Mark release points with `git tag v1.0.0` and push them with `git push --tags`.",
      "Fetch vs pull": "Understand that `git fetch` downloads remote changes without merging, while `git pull` fetches and merges.",
      "Forks": "Understand the fork-and-pull-request workflow used for contributing to repositories you don't own.",
      "Reset vs revert": "Use `git reset` to move the branch pointer (rewriting history) versus `git revert` to add a new commit undoing changes safely.",
      "Reflog": "Recover lost commits using `git reflog` to find and restore a dropped reference.",
      "Cherry-pick": "Apply a specific commit from another branch with `git cherry-pick <commit-hash>`.",
      "Bisect": "Find the commit that introduced a bug using `git bisect start`, marking commits good/bad until it's found.",
      "Hooks": "Automate checks with scripts in `.git/hooks/`, such as a `pre-commit` hook running a linter.",
    },
    exercises: [
      { title: "Make your first commit", prompt: "Initialize a repo, create a file, and commit it with a meaningful message.", hint: "git init, git add file, git commit -m '...'" },
      { title: "Branch and merge", prompt: "Create a feature branch, make a change, and merge it back into main.", hint: "git checkout -b feature, commit, git checkout main, git merge feature." },
      { title: "Resolve a merge conflict", prompt: "Create conflicting changes on two branches and resolve the conflict manually.", hint: "Edit the file to remove <<<<<<< markers, then git add and git commit." },
      { title: "Recover a lost commit", prompt: "Reset a branch hard, then use the reflog to recover the lost commit.", hint: "git reset --hard HEAD~1 then git reflog and git reset --hard <hash>." },
    ],
    projects: [
      { title: "Personal Project History Cleanup", brief: "Take a messy repo and clean up its commit history before sharing it.", steps: ["Review the log with git log --oneline", "Use interactive rebase to squash small commits", "Rewrite unclear commit messages", "Verify the working tree still builds"], skills: ["Rebasing", "Commit hygiene", "Log inspection"] },
      { title: "Feature Branch Workflow", brief: "Simulate a team workflow using feature branches and merges for a small app.", steps: ["Create a main branch with initial code", "Create two feature branches for separate features", "Merge each back into main, resolving any conflicts", "Tag a release once both are merged"], skills: ["Branching", "Merging", "Tagging"] },
      { title: "Pre-commit Quality Gate", brief: "Add a Git hook that prevents commits with failing tests or lint errors.", steps: ["Write a shell script that runs tests/lint", "Place it at .git/hooks/pre-commit and make it executable", "Test that bad commits are blocked", "Document the hook for teammates"], skills: ["Hooks", "Automation", "Scripting"] },
    ],
    resources: [
      { label: "Pro Git Book", url: "https://git-scm.com/book/en/v2" },
      { label: "Git Reference Manual", url: "https://git-scm.com/docs" },
      { label: "GitHub Git Cheat Sheet", url: "https://education.github.com/git-cheat-sheet-education.pdf" },
    ],
  },
  github: {
    intro:
      "GitHub layers collaboration tools — pull requests, issues, and CI automation — on top of Git, making it the standard platform for team and open-source development.",
    lessonNotes: {
      "Repos and README": "Create a repository and write a clear `README.md` explaining the project's purpose and usage.",
      "Issues": "Track bugs and tasks using GitHub Issues, with labels, assignees, and milestones.",
      "Pull requests": "Propose changes with a pull request, describing what changed and why before merging.",
      "Reviews": "Request and give code reviews using inline comments and the Approve/Request changes actions.",
      "Projects": "Organize work visually using GitHub Projects boards linked to issues and pull requests.",
      "Branch protection": "Require passing checks and reviews before merging by configuring branch protection rules.",
      "Conventional commits": "Write structured commit messages like `feat: add login` to enable automated changelogs.",
      "Templates": "Standardize issues and PRs with `.github/ISSUE_TEMPLATE` and `PULL_REQUEST_TEMPLATE.md`.",
      "Releases": "Publish versioned releases with notes and downloadable assets from the Releases tab.",
      "Discussions": "Use GitHub Discussions for Q&A and community conversation separate from issues.",
      "Actions basics": "Automate tasks by adding a workflow file under `.github/workflows/`.",
      "Workflow syntax": "Define triggers, jobs, and steps in YAML using `on:`, `jobs:`, and `steps:`.",
      "CI for tests": "Run your test suite on every push using an Actions workflow with a `run: npm test` step.",
      "Secrets": "Store sensitive values like API keys in repository secrets and access them via `${{ secrets.NAME }}`.",
      "Deploy workflows": "Automate deployments by adding a workflow step that publishes on merge to main.",
      "GitHub Pages": "Host a static site directly from a repository by enabling GitHub Pages in settings.",
      "Profile README": "Create a special README displayed on your profile using a repo named after your username.",
      "Open source etiquette": "Follow contribution guidelines, keep PRs focused, and respond respectfully to feedback.",
      "Licensing": "Choose and add an open-source license file like MIT or Apache 2.0 to clarify usage rights.",
      "Security advisories": "Report and track vulnerabilities privately using GitHub's Security Advisories feature.",
    },
    exercises: [
      { title: "Open your first pull request", prompt: "Fork a repo, make a small change, and open a pull request describing it.", hint: "Fork, clone, branch, commit, push, then open the PR on GitHub." },
      { title: "Set up a CI workflow", prompt: "Add a GitHub Actions workflow that runs tests on every push.", hint: "Create .github/workflows/ci.yml with an 'on: push' trigger and a test step." },
      { title: "Protect the main branch", prompt: "Configure branch protection requiring review and passing CI before merge.", hint: "Go to Settings > Branches and add a rule for main." },
      { title: "Use an issue template", prompt: "Create a bug report issue template with required fields.", hint: "Add a YAML or markdown file under .github/ISSUE_TEMPLATE/." },
    ],
    projects: [
      { title: "Open Source Contribution", brief: "Make a real contribution to an open-source project via pull request.", steps: ["Find a beginner-friendly issue labeled 'good first issue'", "Fork and clone the repository", "Make the fix and write tests if needed", "Open a PR following the project's contribution guide"], skills: ["Pull requests", "Forking", "Collaboration"] },
      { title: "CI/CD Pipeline for a Small App", brief: "Set up an Actions pipeline that tests and deploys a simple web app on merge.", steps: ["Add a workflow that runs on push and pull_request", "Run linting and tests as separate jobs", "Add a deploy job triggered only on main", "Store deployment credentials as secrets"], skills: ["Actions", "CI/CD", "Secrets"] },
      { title: "Team Project Board", brief: "Run a small project using GitHub Issues, Projects, and templates as if managing a team.", steps: ["Set up issue and PR templates", "Create a Projects board with To Do/In Progress/Done", "Link issues to the board and to PRs", "Enforce branch protection for merges"], skills: ["Projects", "Issues", "Branch protection"] },
    ],
    resources: [
      { label: "GitHub Docs", url: "https://docs.github.com/en" },
      { label: "GitHub Actions Documentation", url: "https://docs.github.com/en/actions" },
      { label: "GitHub Skills", url: "https://skills.github.com/" },
    ],
  },
  linux: {
    intro:
      "Linux powers most servers and cloud infrastructure. This course teaches the command line, permissions, processes, and shell scripting needed to administer it.",
    lessonNotes: {
      "Filesystem layout": "Learn the standard Linux directory structure like `/etc`, `/var`, `/home`, and `/usr` and their purposes.",
      "Navigation commands": "Move around with `cd`, `ls`, `pwd`, and inspect paths with `find`.",
      "Files and directories": "Create, copy, move, and remove files using `touch`, `cp`, `mv`, `rm`, and `mkdir`.",
      "Pipes and redirection": "Chain commands with `|`, redirect output with `>`/`>>`, and input with `<`.",
      "grep, sed, awk": "Search text with `grep pattern file`, transform it with `sed 's/old/new/'`, and process columns with `awk '{print $1}'`.",
      "Users and groups": "Manage accounts with `useradd`, `usermod`, and `groupadd`, viewing them in `/etc/passwd`.",
      "chmod and chown": "Control file permissions with `chmod 755 file` and ownership with `chown user:group file`.",
      "sudo": "Run commands as another user, typically root, with `sudo command`, configured via `/etc/sudoers`.",
      "SSH": "Connect to remote machines securely with `ssh user@host` and set up key-based auth with `ssh-keygen`.",
      "Environment": "Set and inspect environment variables with `export VAR=value` and `echo $VAR`.",
      "ps and top": "List running processes with `ps aux` and monitor them live with `top` or `htop`.",
      "Signals": "Send process signals with `kill -SIGTERM pid` or `kill -9 pid` to terminate processes.",
      "systemd units": "Manage services with `systemctl start/stop/enable service` and inspect unit files under `/etc/systemd/system`.",
      "Cron jobs": "Schedule recurring tasks by editing the crontab with `crontab -e` using cron syntax.",
      "Logs": "Inspect system logs with `journalctl` or files under `/var/log`.",
      "Variables": "Declare shell variables in scripts, e.g. `NAME=\"value\"`, and reference them as `$NAME`.",
      "Conditionals and loops": "Use `if`/`then`/`fi` and `for`/`while` loops to control script flow in bash.",
      "Functions": "Define reusable bash functions with `function_name() { ...; }`.",
      "Arguments": "Access script arguments via `$1`, `$2`, and `$@`, and count them with `$#`.",
      "Practical scripts": "Write a script that automates a real task, like backing up a directory or rotating logs.",
    },
    exercises: [
      { title: "Navigate and inspect", prompt: "Use cd, ls, and find to locate all .log files under /var/log.", hint: "Try find /var/log -name '*.log'." },
      { title: "Fix file permissions", prompt: "Given a script that won't execute, fix its permissions so the owner can run it.", hint: "Use chmod +x script.sh or chmod 755 script.sh." },
      { title: "Filter text with grep and awk", prompt: "Extract all unique IP addresses from a log file.", hint: "Combine grep with a regex and awk '{print $1}' | sort -u." },
      { title: "Schedule a cron job", prompt: "Set up a cron job that runs a backup script every day at 2am.", hint: "Add '0 2 * * * /path/backup.sh' via crontab -e." },
    ],
    projects: [
      { title: "Server Setup Script", brief: "Write a bash script that provisions a fresh Linux server for a web app.", steps: ["Create a non-root user with sudo access", "Install required packages", "Configure a systemd service for the app", "Set up a firewall and SSH key-only login"], skills: ["Bash scripting", "systemd", "Users and permissions"] },
      { title: "Log Analysis Toolkit", brief: "Build a set of shell one-liners and scripts to analyze web server logs.", steps: ["Parse access logs with awk and grep", "Count requests per IP and per status code", "Identify the top 10 most requested paths", "Wrap the analysis in a reusable script with arguments"], skills: ["grep/sed/awk", "Scripting", "Text processing"] },
      { title: "Automated Backup System", brief: "Create a cron-scheduled backup system with rotation and logging.", steps: ["Write a script that archives a directory with tar", "Add timestamped filenames and a retention policy", "Log successes and failures to a file", "Schedule it with cron and verify it runs"], skills: ["Cron", "Bash scripting", "Logs"] },
    ],
    resources: [
      { label: "The Linux Documentation Project", url: "https://tldp.org/" },
      { label: "GNU Bash Reference Manual", url: "https://www.gnu.org/software/bash/manual/bash.html" },
      { label: "systemd Documentation", url: "https://www.freedesktop.org/software/systemd/man/latest/systemctl.html" },
    ],
  },
  networking: {
    intro:
      "Networking explains how data actually travels between machines — from IP addressing and routing to DNS, TCP, and HTTPS — the foundation for debugging real systems.",
    lessonNotes: {
      "OSI and TCP/IP models": "Compare the 7-layer OSI model with the practical 4-layer TCP/IP model used on the real internet.",
      "IP addressing": "Understand IPv4 addresses, classes, and notation like `192.168.1.10`, plus a brief look at IPv6.",
      "Subnets": "Calculate subnet ranges using CIDR notation like `/24` and understand subnet masks.",
      "Routing": "Learn how routers forward packets between networks using routing tables and default gateways.",
      "NAT": "Understand how Network Address Translation lets many private IPs share one public IP.",
      "TCP vs UDP": "Compare connection-oriented, reliable TCP against fast, connectionless UDP and when to use each.",
      "Ports and sockets": "Understand how ports identify services on a host, e.g. port 80 for HTTP, and how sockets pair IP+port.",
      "DNS": "Learn how domain names resolve to IP addresses through recursive resolvers and record types like A, AAAA, and CNAME.",
      "DHCP": "Understand how DHCP automatically assigns IP addresses to devices joining a network.",
      "ICMP and ping": "Use ICMP-based tools like `ping` to test reachability and measure round-trip latency.",
      "HTTP/HTTPS": "Understand HTTP request/response structure, methods, status codes, and how HTTPS adds encryption via TLS.",
      "TLS handshake": "Trace the TLS handshake steps: ClientHello, certificate exchange, and key negotiation.",
      "Cookies and headers": "Understand how cookies maintain state and how headers like `Content-Type` and `Authorization` control requests.",
      "CDNs": "Learn how content delivery networks cache content close to users to reduce latency.",
      "Load balancing": "Understand how load balancers distribute traffic across servers using algorithms like round-robin.",
      "dig and nslookup": "Query DNS records directly using `dig example.com` or `nslookup example.com`.",
      "traceroute": "Trace the path packets take to a destination using `traceroute` (or `tracert` on Windows).",
      "netstat and ss": "List active connections and listening ports using `ss -tulpn` or the older `netstat`.",
      "tcpdump basics": "Capture and inspect live packets with `tcpdump -i eth0 port 80`.",
      "Firewalls": "Control traffic with firewall rules using tools like `iptables` or `ufw`.",
    },
    exercises: [
      { title: "Calculate a subnet", prompt: "Given the network 192.168.1.0/24, determine the usable host range and broadcast address.", hint: "A /24 has 256 addresses; the first is network, the last is broadcast." },
      { title: "Resolve a domain manually", prompt: "Use dig to find the A record and the authoritative name servers for a domain.", hint: "Try dig example.com and dig example.com NS." },
      { title: "Trace a network path", prompt: "Use traceroute to see the hops between your machine and a public website.", hint: "Run traceroute example.com and inspect the hop latencies." },
      { title: "Inspect open ports", prompt: "List all listening TCP ports on your machine and identify the owning process.", hint: "Use ss -tulpn or netstat -tulpn." },
    ],
    projects: [
      { title: "Home Network Map", brief: "Document your home or lab network's topology, addressing, and routing.", steps: ["List all devices and their IP addresses", "Identify the subnet mask and gateway", "Run traceroute to an external site and explain each hop", "Diagram the topology including router and switches"], skills: ["IP addressing", "Subnets", "Routing"] },
      { title: "DNS Troubleshooting Toolkit", brief: "Build a checklist and script for diagnosing DNS resolution failures.", steps: ["Write commands to check A, CNAME, and NS records with dig", "Test resolution against multiple DNS servers", "Check for propagation delay after a DNS change", "Document common failure causes and fixes"], skills: ["DNS", "dig/nslookup", "Troubleshooting"] },
      { title: "Traffic Capture and Analysis", brief: "Capture and analyze HTTP traffic to understand the request/response cycle.", steps: ["Capture traffic on port 80/443 with tcpdump", "Identify the TCP handshake in the capture", "Locate the TLS handshake for an HTTPS request", "Summarize findings including headers observed"], skills: ["tcpdump", "HTTP/HTTPS", "TLS handshake"] },
    ],
    resources: [
      { label: "MDN: How the web works", url: "https://developer.mozilla.org/en-US/docs/Learn_web_development/Getting_started/Web_standards/How_the_web_works" },
      { label: "Cloudflare Learning Center: What is DNS", url: "https://www.cloudflare.com/learning/dns/what-is-dns/" },
      { label: "IETF RFC 793 (TCP)", url: "https://www.rfc-editor.org/rfc/rfc793" },
    ],
  },
};

export default batch3;
export { batch3 };
